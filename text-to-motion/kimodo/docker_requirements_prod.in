#
# Production inference dependencies for Kimodo MCP Server.
# Compile: uv pip compile docker_requirements_prod.in -o docker_requirements_prod.txt
#
# NOTE: torch is listed here because we use a minimal CUDA runtime base image
# instead of the NGC PyTorch image. Install with:
#   pip install -r docker_requirements_prod.txt --extra-index-url https://download.pytorch.org/whl/cu124
#

# PyTorch
torch>=2.1.0

# Config
hydra-core>=1.3
omegaconf>=2.3

# Core numerics
numpy>=1.23,<2
scipy>=1.10,<2

# Model / embeddings (LLM2Vec text encoder)
transformers==5.1.0
urllib3>=2.6.3
boto3
peft>=0.12
einops>=0.7

# Misc
tqdm>=4.0
packaging>=21.0
pydantic>=2.0
filelock>=3.20.3

# MCP Server
fastmcp>=3.0

# Local packages
./MotionCorrection
-e .
