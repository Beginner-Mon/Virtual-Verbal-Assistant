# Core framework
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-openai>=1.0.0          # DeepSeek (OpenAI-compatible API)
langchain-google-genai>=2.0.0    # legacy / fallback, keep for now
langchain-mcp-adapters>=0.1.0
mcp>=1.0.0
pydantic>=2.0

# LLM
anthropic>=0.34.0

# Database
asyncpg>=0.29.0
pgvector>=0.3.0
alembic>=1.13.0

# SSE streaming
sse-starlette>=2.0.0

# HTTP client (for Kimodo/VieNeu-TTS REST calls)
httpx>=0.27.0

# Redis — STM in memory_node + Celery broker (already in env; declare for new machines)
redis>=5.0.0

# .env loader (read GEMINI_API_KEY[S] from agenticRAG/.../.env)
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
