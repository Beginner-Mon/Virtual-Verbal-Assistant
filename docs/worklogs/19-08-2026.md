---
date: 2026-08-19
tags: [worklog, neon, tts, voice-cloning, i18n, persona]
author: K
branch: feature/langgraph-rewrite
---

# Worklog 19-08-2026 — K

Ba việc N giao: (1) DSN Neon mới, (2) map model ↔ voices, (3) Anne chỉ nói tiếng
Việt. Cả ba xong. Còn chặn đúng một thứ: **8 file `.wav` N tự bổ sung**.

---

## 1. Neon `us-east-1` — nghiệm thu sạch

N đưa DSN **pooled**. Đã chuyển sang **direct** (bỏ `-pooler`, **giữ `.c-11`**)
rồi ghi vào `agenticRAG/.env`.

> ⚠️ Pooled cũng connect được, nên rất dễ dùng nhầm. Nó chỉ hỏng khi asyncpg cần
> prepared statement — tức là hỏng lúc chạy thật, không hỏng lúc test.

| Tiêu chí | Kỳ vọng | Thực tế |
| --- | --- | --- |
| PostgreSQL / pgvector | 18.4 / 0.8.6 | ✅ |
| `kb_embeddings` | 2918 | ✅ 2918 |
| vector dims | 384 | ✅ 384 |
| `idx_kb_emb_embedding` | có | ✅ |
| `documents.metadata->>'has_description'` | 1368 | ✅ 1368 |
| `characters` | 4 | ✅ anne, bronya, hatsune-miku, miki |
| alembic | mới nhất | ✅ `007_rls` |

**Không mất dữ liệu.** Tri làm sạch.

*(`has_description` là khoá JSONB trong `documents.metadata`, không phải cột —
query trong `docs/ops/neon-migration-us-east-1.md` viết đúng ngay từ đầu.)*

---

## 2. Ngôn ngữ câu trả lời — N nhớ đúng, nhưng field không dùng được

N nói "response API chắc chắn có field ngôn ngữ". Tôi trước đó bảo chỉ có hai
field — **đếm sai**. Có ba, và không cái nào dùng được:

| Chỗ | Có thật? | Vì sao không dùng được |
| --- | --- | --- |
| `SpeechLLm /synthesize` → `"language"` | ✅ có | **Echo lại field trong request.** `client.py` chưa bao giờ gửi ⇒ vĩnh viễn `"en"` |
| `_get_user_profile()` → `{"age","language"}` | ❌ đã xoá | Hàm xoá ở `492d8a4`, cột `users.profile` cũng đã drop. Vết tích: mock ở `run_ollama_node_smoke.py:269` — script đó **đang hỏng** |
| `voice_identity.language` / `characters.voice_language` | ✅ có | Cố định theo nhân vật, cả 4 đều `vi` |

Gọi thật DeepSeek để chốt: `['id','object','created','model','choices','usage',
'system_fingerprint']` — **không có field ngôn ngữ**. Schema OpenAI-compatible,
DeepSeek không nằm trong đường đi của `/synthesize` (SpeechLLm không hề nói
chuyện với DeepSeek).

⇒ Field `language` là **cái ống, không phải cảm biến**. Vẫn dùng — giờ có gửi
xuống, nên tên file hết sai — nhưng nguồn phải tự quyết.

---

## 3. Detector `shared/lang.py`

Điểm thiết kế: **tín hiệu mạnh là dấu NÀO, không phải bao nhiêu dấu.**

Tập nhận diện **cố ý loại** `é è ê à â ô î û ï ç ü ñ` — đúng những dấu từ mượn
tiếng Anh mang theo (`café`, `résumé`, `naïve`, `façade`, `crème fraîche`,
`pâté`, `jalapeño`). Loại luôn `ã õ` vì tiếng Bồ có (`São Paulo`). Còn lại là
thứ không chính tả Latin nào khác có: móc `ơ ư`, dấu hỏi, dấu nặng, chồng dấu
`ấ ế ố`, và `đ`.

Ba tầng:

1. **Tỷ lệ từ mang dấu Việt ≥ 15% VÀ dày hơn mật độ hư từ tiếng Anh.**
   Điều kiện thứ hai không bỏ được: `"The Vietnamese term is 'đau lưng'"` = 0.22,
   `"Bài tập này gọi là 'Bird Dog'"` = 0.20 — **cách nhau 0.02, không ngưỡng nào
   tách được**, chỉ so hai mật độ mới đúng.
2. **Từ khoá không dấu** cho tiếng Việt viết không dấu. Đã loại mọi từ trùng
   tiếng Anh (`the, ban, bi, can, cam, chi, dang, den, em, hay, hit, lan, lung,
   ma, may, no, on, qua, rat, so, tap, to, tot, vi`). Bản nháp đầu để sót `the`
   → cả câu tiếng Anh bị chấm là tiếng Việt.
3. **Câu hỏi làm trọng tài.** `"OK"` / `"Có."` không có tín hiệu — không đoán
   bừa, lấy ngôn ngữ câu hỏi. Hết đường thì `voice_language` của nhân vật.

Đo: **0.011 ms** câu ngắn, **0.25 ms** câu lâm sàng 1608 ký tự. TTS mất 30–45 s.

**Rủi ro còn lại (ghi để sau khỏi ngạc nhiên):** ngưỡng 15% được chỉnh trên chính
bộ case này. Ca xấu là **câu trả lời tiếng Anh ngắn có trích cụm tiếng Việt**.
Giảm thiểu bằng tầng 3 + log ghi rõ `lang / lý do / file wav`.

---

## 4. Map model ↔ voices

```
voices/<slug>_<lang>.wav        voices/anne_vi.wav, voices/anne_en.wav
```

Suy ra từ `persona_id`, **không đọc `voice_identity.voice_path` nữa** — field đó
ghim một file cho một nhân vật, không diễn tả được "nhân vật này, tiếng Anh". Đã
xoá khỏi cả 3 file `eca_*` còn sót.

Chuỗi khoá có sẵn, không dựng mới:
`UI selectedVrmId → persona_id → characters.slug → voices/<slug>_<lang>.wav`

### Điểm kiến trúc quan trọng nhất

> **Việc kiểm "file có tồn tại không" nằm ở SpeechLLm, không ở LangGraph.**

LangGraph dựng *cái tên*; SpeechLLm resolve nó theo **root của chính nó** (không
theo CWD — uvicorn khởi động ở đâu không được đổi kết quả). Hai process, deploy
xong là hai máy. Check ở LangGraph là check nhầm ổ đĩa: local thì đúng, tách máy
thì thành lời nói dối.

`vieneu_client._resolve_voice` trước đây gọi thẳng `encode_reference()` — thiếu
file là **ném exception**. Giờ: thiếu file → **WARNING kèm đường dẫn tuyệt đối**
→ preset. Nhân vật chưa có giọng vẫn nói được.

`voice_tag` trong log cũng sai: nó ghi `"custom"` mỗi khi có `voice_path`, tức là
đúng cái ca file thiếu và đã âm thầm tụt về preset. Giờ ghi theo **thứ thật sự
dùng**.

### `.gitignore`

`SpeechLLm/.gitignore` có `*.wav` **hai lần** (dòng ~35 và ~105) dưới mục
*"Temporary audio files"*. Rule đó gom cả file voice tham chiếu, mà git không
track thư mục rỗng ⇒ **người clone không thấy `voices/` đâu cả** — đúng thứ Tri
báo. Đã thêm `!voices/` + `!voices/*.wav` ở **cuối file** (negation phải nằm sau
mọi exclusion). `data/temp_audio/` vẫn bị chặn — đã verify bằng `git check-ignore`.

---

## 5. Anne nói tiếng Việt — chẩn đoán đầu của tôi **sai**

Tôi đã nói: xoá cụm *"kèm giải thích tiếng Việt"* ở `anne.md:13` là đủ.
**Test rồi — không đủ.** Anne vẫn trả lời tiếng Việt.

Hỏi thật DeepSeek bằng tiếng Anh với đúng persona prompt của production:

| Nhân vật | `_LANGUAGE_RULE` cũ | Sau khi tăng cường |
| --- | --- | --- |
| anne | ❌ tiếng Việt | ✅ English |
| **hatsune-miku** | ❌ tiếng Việt | ✅ English |
| bronya | ✅ English | ✅ English |
| miki | ✅ English | ✅ English |

**Tri chỉ báo Anne — Miku cũng hỏng.** Điểm chung: hai nhân vật này có luật gắn
chặt vào tiếng Việt (Anne: *"giải thích tiếng Việt"*; Miku: *"Xưng 'mình' và gọi
người dùng là 'bạn'"*). Toàn bộ persona prompt viết bằng tiếng Việt mới là lực
kéo — một dòng không lật được.

Bản vá nằm ở **`_LANGUAGE_RULE`, một chỗ, áp cho cả 4**, không phải viết lại tính
cách nhân vật:

> *the persona rules are WRITTEN in Vietnamese. That is the language of the
> instructions, NOT the language of your reply.*

### Safety templates — lỗi chung cả 4, Tri chưa gặp

Grader là **rule-based, không gọi LLM**: nó chèn template **nguyên văn**. Nên câu
trả lời tiếng Anh bị dán cảnh báo tiếng Việt — tệ nhất đúng ở `red_flag_screen`,
dòng người đọc không được phép bỏ qua. Chỉ lộ khi trúng red flag, nên chưa ai báo.

Đã thêm biến thể `.en` cho cả 4 nhân vật + `DEFAULT_SAFETY_TEMPLATES_EN` +
`_UNAUTHORIZED_DISCLAIMER_EN`. Thứ tự tra:

```
persona <tag>.<lang>  →  persona <tag>  →  DEFAULT theo lang  →  ""
```

Persona `<tag>` đứng **trên** default tiếng Anh có chủ ý: nhân vật đã tùy biến
cảnh báo là có lý do, thay lặng lẽ bằng boilerplate là làm mất nó.

`get_safety_text(tag, persona_id, lang="vi")` — **default `"vi"`** nên 5 test
grader vừa sửa hôm 17/08 không gãy lại.

### 🔴 Lỗi thứ hai tìm thấy: `eca_default` cảnh báo bằng tiếng Anh

`eca_default` là persona **fallback** khi `persona_id` sai hoặc thiếu — và safety
templates của nó **chỉ có tiếng Anh**. Tức là đường fallback trả lời tiếng Việt
rồi cảnh báo tiếng Anh. Đã đảo: plain = tiếng Việt, `.en` = bản tiếng Anh cũ.

*(`eca_clinical` / `eca_friendly` không đụng — không có trong DB, không có trên
UI. Chỉ xoá `voice_path` chết.)*

---

## 6. `scripts/sync_personas_to_db.py` (mới) — bắt buộc, không phải tiện ích

`preload_personas_from_db()` nạp `characters.persona` vào cache lúc khởi động, và
`get_persona()` đọc cache **trước** file. ⇒ **Sửa MD mà không sync thì ở đâu có DB
là ở đó không đổi gì.** File trông có thẩm quyền mà thật ra không.

Không dùng `upload_characters_to_s3.py` vì script đó **ghi đè `vrm_url` theo
`--cdn`** — bắt người vận hành nhập lại CloudFront URL cho một thay đổi markdown
là mời gọi gõ sai và trỏ nhầm toàn bộ avatar. Script mới đụng đúng 2 cột.

Đã chạy: 4 row updated, chạy lại → `unchanged` (idempotent).

---

## 7. Test

| Bộ | Số test |
| --- | --- |
| `test_lang_detect.py` | 57 |
| `test_voice_selection.py` | 15 |
| `test_safety_templates_bilingual.py` | 36 |
| `tests/SpeechLLm/test_voice_fallback.py` | 5 |
| **Tổng mới** | **113** |

**0 regression.** Đã đối chứng bằng `git stash` chạy lại ở HEAD: 34 failure hiện
tại **trùng khít** 34 failure có sẵn trước khi tôi động vào (thiếu `tyro`,
`soundfile.__spec__`, ElevenLabs/Coqui đã bỏ, v.v.).

> Test không chỉ assert kết quả mà assert **thiết kế**: `_VI_MARKERS ∩ _EN_MARKERS
> = ∅`, và mỗi dấu từ mượn `é è ê à â ô î û ï ç ü ñ ã õ` phải **không** nằm trong
> tập nhận diện. Thêm lại một cái là mọi chữ `café` bị đọc bằng giọng Việt.

---

## Còn lại

- 🔴 **N bỏ 8 file `.wav` vào `SpeechLLm/voices/`**: `<slug>_vi.wav` và
  `<slug>_en.wav` cho `anne`, `bronya`, `hatsune-miku`, `miki`. Thiếu file nào
  thì nhân vật đó rơi về preset kèm WARNING — không lỗi.
- `reporter_vi.wav` / `seele_en.wav` đang có **không khớp slug nào** → không được
  dùng. Đổi tên hoặc thu mới.
- Frontend **không cần sửa gì** — nó đã gửi `persona_id` đúng rồi.
- Chưa restart backend `:8000`.
