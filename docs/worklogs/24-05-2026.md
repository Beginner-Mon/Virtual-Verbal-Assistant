---
title: "Phase 3 — MCP Servers + TTS at FastAPI Layer"
date: 2026-05-24
tags: [phase-3, mcp, kimodo, web-search, tts, fastapi, langgraph]
participants: [N (Developer)]
---

# 24/05/2026 — Phase 3 Implementation

## Summary

Implemented Phase 3 per K's PHASE-3.md spec. 9 tasks, ~6h actual (vs 8h estimated).

## Task Log

### 3.1 Cleanup v2.2 leftovers (30m)
- Deleted `services/kimodo/` (replaced by MCP server)
- Deleted `streaming/` (approval gate removed in v2.4)
- Refactored `services/vieneu_tts/tasks.py` — removed Redis pub/sub, simplified to `_persist()` with 1h TTL
- Updated `celery_app.py` — removed kimodo task import
- 38/38 Phase 2.5 tests green

### 3.2 Kimodo MCP server (mock) (45m)
- Created `langgraph_agents/mcp/kimodo_server.py`
- Mock `_generate_motion_mock()` returns `_mock: True`, valid video_url, joints passthrough
- `_USE_REAL` flag ready for NVIDIA Kimodo plug
- Verified: tool list contains `generate_motion`, mock returns correct shape

### 3.3 Web search MCP server (DuckDuckGo) (30m)
- Created `langgraph_agents/mcp/web_search_server.py`
- Lazy import of `duckduckgo_search.DDGS` to avoid blocking imports
- Tool `search_medical` with domain_filter support
- Added `duckduckgo-search>=6.0.0` to requirements-langgraph.txt

### 3.4 MCP config (15m)
- Created `config/mcp_servers.yaml` — stdio transport for dev, HTTP commented for production

### 3.5 MCP client wrapper + wire into retriever (60m)
- Created `langgraph_agents/mcp/client.py` — lazy singleton, async `get_mcp_tools()`
- Updated `retriever_agent.py` — `_build_tools()` async, appends MCP tools to `[pgvector_search]`
- Updated `graph.py` — `build_graph_async()` + sync `build_graph()` wrapper
- Updated `test_phase2_5_integration.py` — `build_graph()` → `await build_graph_async()`
- Verified: MCP tools discovered (generate_motion, search_medical), graph compiles

### 3.6 Grader visualize_motion verify (5m)
- Grader unchanged. Existing tests confirm rule fires correctly
- `test_grader_visualize_motion_no_tool_fails` → fails on missing ToolMessage
- `test_grader_visualize_motion_with_tool_passes` → passes with generate_motion ToolMessage

### 3.7 VieNeu-TTS Celery refactor (20m)
- Done in 3.1 — removed Redis pub/sub, kept `voice_path` param, `_persist()` with 1h TTL
- Queue: `langgraph`, Redis DB 1

### 3.8 FastAPI /chat endpoint (60m)
- Created `langgraph_agents/api/`: `__init__.py`, `schemas.py`, `main.py`
- `create_app()` factory pattern (fastapi 0.128.0 + starlette 1.0.1 incompatibility)
- POST /chat → run graph → optional TTS fire-and-forget
- GET /health → service status
- GET /tts/{task_id}/result → Redis poll (Phase 5 SSE will replace)
- Lifespan: loads graph at startup, warms Redis
- Run: `uvicorn langgraph_agents.api.main:create_app --factory --reload --port 8080`

### 3.9 Tests (90m)
Created 6 test files:
- `test_phase3_mcp_kimodo.py` — 4 tests (list, mock returns, constraints, empty)
- `test_phase3_mcp_web_search.py` — 3 tests (list, returns results [integration], handles failure)
- `test_phase3_mcp_client.py` — 2 tests (caches, empty config)
- `test_phase3_retriever_with_mcp.py` — 1 test (graph with MCP tools, requires LLM key)
- `test_phase3_tts_task.py` — 2 tests (persists result, service down)
- `test_phase3_api.py` — 2 schema tests + 4 API tests (skipped due to FastAPI/starlette incompatibility)

## Test Results

```
48 passed, 5 skipped, 6 deselected (integration)
```

Skips:
- 4 API tests: FastAPI 0.128.0 + starlette 1.0.1 version mismatch in firstconda
- 1 web search: duckduckgo-search not installed

## Known Gaps

| Issue | Resolution |
|-------|----------|
| `duckduckgo-search` not installed | `pip install duckduckgo-search` in firstconda |
| FastAPI 0.128.0 incompatible with starlette 1.0.1 | Upgrade fastapi or downgrade starlette |
| API tests skip due to version mismatch | Will pass once deps resolved |
| `test_full_graph_v24_conversation_path` flaky | Pre-existing: LLM returns `clarify` for "Xin chào" |

## Files Changed

### Created (10)
- `langgraph_agents/mcp/__init__.py`
- `langgraph_agents/mcp/kimodo_server.py`
- `langgraph_agents/mcp/web_search_server.py`
- `langgraph_agents/mcp/client.py`
- `langgraph_agents/api/__init__.py`
- `langgraph_agents/api/main.py`
- `langgraph_agents/api/schemas.py`
- `config/mcp_servers.yaml`
- `tests/langgraph_agents/test_phase3_*.py` (6 files)

### Modified (4)
- `langgraph_agents/graph.py` — async build
- `langgraph_agents/nodes/retriever_agent.py` — async tools
- `langgraph_agents/services/vieneu_tts/tasks.py` — remove pub/sub
- `langgraph_agents/celery_app.py` — remove kimodo import
- `requirements-langgraph.txt` — +duckduckgo-search, +uvicorn
- `tests/langgraph_agents/test_phase2_5_integration.py` — async graph

### Removed (3)
- `langgraph_agents/services/kimodo/` (entire folder)
- `langgraph_agents/streaming/` (entire folder)

## Next: CP review by K

Checkpoints ready:
- **CP1**: Kimodo MCP working (3.2) ✓
- **CP2**: MCP tools wired into graph (3.5) ✓ — `generate_motion` and `search_medical` discovered
- **CP3**: FastAPI /chat endpoint ready (3.8) ✓ — needs `duckduckgo-search` and fastapi fix
- **CP4**: Test results — 48 pass, 0 fail ✓

## Unblocks

- Phase 5 SSE streaming: /chat endpoint exists, just replace JSON with `astream_events()`
- Phase 5 Frontend: API contract locked
- Real Kimodo: Plug into `_generate_motion_real()`
