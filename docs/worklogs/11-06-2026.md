---
date: 2026-06-11
phase: rebuild-testing
branch: feature/langgraph-rewrite
participants: [K (Architect), N (Developer)]
mode: test-fix (delete old tests, write new tests, fix bugs, integration)
---

# 11/06/2026 — Test suite rebuild: 33 failures → 187/187 passed

## Context

Sau rebuild 15 bước M.9 (06/06), test suite có 4 file test cũ reference API đã chết
(`intent/confidence/expanded_query/memory_context/_check_rules/_needs_recall`), cộng thêm nhiều bug
trong code mới chưa được phát hiện.

Mục tiêu: dọn sạch test suite, viết test mới khớp 3-axis model, fix tất cả bug phát hiện được,
bật Docker PostgreSQL + Redis để chạy integration test.

## 1. Xóa + viết lại test files

### Xóa (reference API cũ đã chết)

| File | Lý do |
|---|---|
| `test_phase2_5_planner.py` | Import `intent`, `confidence`, `expanded_query`, `search_strategy`, `memory_context` |
| `test_phase2_5_grader.py` | Import `_check_rules` (đã đổi thành `_grade_tags`) |
| `test_phase2_5_memory.py` | Import `_needs_recall` (không tồn tại) |
| `test_phase2_5_retriever.py` | Reference `needs_web_search`, `needs_kb_search` (đã bỏ D2b) |

### Viết mới (60+ unit tests với mock Postgres/Redis)

| File | Tests | Nội dung |
|---|---|---|
| `test_phase2_5_planner.py` | 18 | PlanOutput schema (9), PLANNER_TAGS vocab (3), planner_node LLM mock (6) |
| `test_phase2_5_memory.py` | 19 | Context assembly (6), token budget (4), mock DB/Redis loaders (9) |
| `test_phase2_5_retriever.py` | 18 | Routing (6), planner routing (5), tool config (3), error routing (3), retriever_node mock (3) |
| `test_phase2_5_grader.py` | 20 | Rule edge cases (13), _grade_tags edge cases (10), grader_node (5) |

## 2. Bug fixes phát hiện qua test

| # | Bug | File | Fix |
|---|---|---|---|
| 1 | `config: dict` không được LangGraph 1.2.4 nhận diện là config injection | 5 node files | `config: dict` → `config: RunnableConfig` (import từ `langchain_core.runnables`) |
| 2 | `planner_node` crash khi `plan is None` (breaker open) | `nodes/planner.py` | Thêm `if plan is None` guard → safe fallback `needs_clarification=True` |
| 3 | `session_store.py` dùng schema cũ (JSONB messages, intent/tokens/grader_result columns) | `db/session_store.py` | Rewrite khớp M.4: messages bảng riêng, conversations PK=session_id, token_count |
| 4 | `api/main.py` gọi `write_session_turn(intent=...)` | `api/main.py` | `intent` → `total_tokens`, bỏ param `intent` |
| 5 | `api/schemas.py` ChatResponse có `intent/confidence/grader_warning` cũ | `api/schemas.py` | Thay bằng `required_outputs/needs_retrieval/needs_motion` |
| 6 | SSE stream dùng `intent` thay `required_outputs` | `api/main.py` | Sửa `extra["intent"]` → `extra["required_outputs"]`, `final_state["intent"]` → `final_state["required_outputs"]` |
| 7 | Circuit breaker test dùng `state = {"memory_context": {}}` cũ | `test_phase6_circuit_breaker.py` | Sửa state sang AgentState mới + clear lru_cache `get_chat_model` để breaker mới có hiệu lực |
| 8 | `test_phase2_5_tools.py` import `pgvector_search` đã đổi tên | `test_phase2_5_tools.py` | `pgvector_search` → `kb_search` |
| 9 | TTS test Redis mock: `MagicMock` → `await None` crash | `test_phase3_api.py` | `mock_redis.get = AsyncMock(return_value=...)` thay vì `MagicMock` |

## 3. Docker + Alembic

### Docker PostgreSQL
- Phát hiện host có `postgresql-x64-18` Windows service chiếm port 5432
- Đổi Docker port mapping: `5433:5432`
- Sửa tất cả DSN: `config/langgraph.yaml`, `alembic.ini`, `alembic/env.py`, `db/postgres.py`

### Alembic migration
- `op.execute(multi_statement_SQL)` → asyncpg không hỗ trợ multi-statement trong prepared statement
- Fix: split SQL thành từng câu riêng qua `_split_sql()` helper, chạy từng `op.execute()` một
- Migration thành công: 7 tables M.4 + alembic_version

## 4. Model embedding

- Cài `sentence-transformers` + tải `intfloat/multilingual-e5-small` (384 dims)
- Model hoạt động, embed query/passage thành công

## 5. Test results

```
187 passed in 213.86s
  - 21 unit tests (no LLM)
  - 3 integration tests (LLM, no DB)
  - 2 integration tests (LLM + PostgreSQL + Redis + embedding)
  - 161 other tests (SSE, MCP, circuit breaker, health, logging, etc.)
```

## Còn thiếu (defer, không block)

1. **GitHub Actions CI** — chưa có CI pipeline cho pytest
2. **Eval dataset** — chưa có golden test cho prompt regression
3. **Auth layer** — vẫn dùng `user_id = "anonymous"` (Phase 7)
4. **LLM fallback** — chưa có multi-provider (chỉ DeepSeek)
