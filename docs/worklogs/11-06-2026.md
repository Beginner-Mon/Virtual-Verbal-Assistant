---
date: 2026-06-11
phase: rebuild-testing
branch: feature/langgraph-rewrite
participants: [K (Architect), N (Developer)]
mode: test-fix (delete old tests, write new tests, fix bugs, integration)
---

# 11/06/2026 — Test suite rebuild: 33 failures → 187/187 passed

## Context

Sau rebuild 15 bước M.9 (06/06), test suite có 4 file test cũ reference API đã chết
(`intent/confidence/expanded_query/memory_context/_check_rules/_needs_recall`), cộng thêm nhiều bug
trong code mới chưa được phát hiện.

Mục tiêu: dọn sạch test suite, viết test mới khớp 3-axis model, fix tất cả bug phát hiện được,
bật Docker PostgreSQL + Redis để chạy integration test.

## 1. Xóa + viết lại test files

### Xóa (reference API cũ đã chết)

| File | Lý do |
|---|---|
| `test_phase2_5_planner.py` | Import `intent`, `confidence`, `expanded_query`, `search_strategy`, `memory_context` |
| `test_phase2_5_grader.py` | Import `_check_rules` (đã đổi thành `_grade_tags`) |
| `test_phase2_5_memory.py` | Import `_needs_recall` (không tồn tại) |
| `test_phase2_5_retriever.py` | Reference `needs_web_search`, `needs_kb_search` (đã bỏ D2b) |

### Viết mới (60+ unit tests với mock Postgres/Redis)

| File | Tests | Nội dung |
|---|---|---|
| `test_phase2_5_planner.py` | 18 | PlanOutput schema (9), PLANNER_TAGS vocab (3), planner_node LLM mock (6) |
| `test_phase2_5_memory.py` | 19 | Context assembly (6), token budget (4), mock DB/Redis loaders (9) |
| `test_phase2_5_retriever.py` | 18 | Routing (6), planner routing (5), tool config (3), error routing (3), retriever_node mock (3) |
| `test_phase2_5_grader.py` | 20 | Rule edge cases (13), _grade_tags edge cases (10), grader_node (5) |

## 2. Bug fixes phát hiện qua test

| # | Bug | File | Fix |
|---|---|---|---|
| 1 | `config: dict` không được LangGraph 1.2.4 nhận diện là config injection | 5 node files | `config: dict` → `config: RunnableConfig` (import từ `langchain_core.runnables`) |
| 2 | `planner_node` crash khi `plan is None` (breaker open) | `nodes/planner.py` | Thêm `if plan is None` guard → safe fallback `needs_clarification=True` |
| 3 | `session_store.py` dùng schema cũ (JSONB messages, intent/tokens/grader_result columns) | `db/session_store.py` | Rewrite khớp M.4: messages bảng riêng, conversations PK=session_id, token_count |
| 4 | `api/main.py` gọi `write_session_turn(intent=...)` | `api/main.py` | `intent` → `total_tokens`, bỏ param `intent` |
| 5 | `api/schemas.py` ChatResponse có `intent/confidence/grader_warning` cũ | `api/schemas.py` | Thay bằng `required_outputs/needs_retrieval/needs_motion` |
| 6 | SSE stream dùng `intent` thay `required_outputs` | `api/main.py` | Sửa `extra["intent"]` → `extra["required_outputs"]`, `final_state["intent"]` → `final_state["required_outputs"]` |
| 7 | Circuit breaker test dùng `state = {"memory_context": {}}` cũ | `test_phase6_circuit_breaker.py` | Sửa state sang AgentState mới + clear lru_cache `get_chat_model` để breaker mới có hiệu lực |
| 8 | `test_phase2_5_tools.py` import `pgvector_search` đã đổi tên | `test_phase2_5_tools.py` | `pgvector_search` → `kb_search` |
| 9 | TTS test Redis mock: `MagicMock` → `await None` crash | `test_phase3_api.py` | `mock_redis.get = AsyncMock(return_value=...)` thay vì `MagicMock` |

## 3. Docker + Alembic

### Docker PostgreSQL
- Phát hiện host có `postgresql-x64-18` Windows service chiếm port 5432
- Đổi Docker port mapping: `5433:5432`
- Sửa tất cả DSN: `config/langgraph.yaml`, `alembic.ini`, `alembic/env.py`, `db/postgres.py`

### Alembic migration
- `op.execute(multi_statement_SQL)` → asyncpg không hỗ trợ multi-statement trong prepared statement
- Fix: split SQL thành từng câu riêng qua `_split_sql()` helper, chạy từng `op.execute()` một
- Migration thành công: 7 tables M.4 + alembic_version

## 4. Model embedding

- Cài `sentence-transformers` + tải `intfloat/multilingual-e5-small` (384 dims)
- Model hoạt động, embed query/passage thành công

## 5. Test results

```
187 passed in 213.86s
  - 21 unit tests (no LLM)
  - 3 integration tests (LLM, no DB)
  - 2 integration tests (LLM + PostgreSQL + Redis + embedding)
  - 161 other tests (SSE, MCP, circuit breaker, health, logging, etc.)
```

## Còn thiếu (defer, không block)

1. **GitHub Actions CI** — chưa có CI pipeline cho pytest
2. **Eval dataset** — chưa có golden test cho prompt regression
3. **Auth layer** — vẫn dùng `user_id = "anonymous"` (Phase 7)
4. **LLM fallback** — chưa có multi-provider (chỉ DeepSeek)
# Worklog — 11-06-2026

**Branch:** `feature/langgraph-rewrite`
**Role:** K (plan) + N (implement)
**Session focus:** DynamoDB Account Linking v5 — rewrite to meet plan v4 spec

---

## Decisions Made

### 1. DynamoDB Schema — appUserId as UUID PK

- PK = `appUserId` (UUID v4, `crypto.randomUUID()`), không dùng cognitoSub
- Fields: `appUserId`, `emailSub`, `googleSub`, `email`, `displayName`, `createdAt`
- GSIs: `emailSub-index`, `googleSub-index`, `email-index`
- 1 email → 1 row duy nhất (TransactWriteItems + EmailLocks table đảm bảo)

### 2. Race-proof email uniqueness

Dùng `EmailLocks` table + `TransactWriteItems`:

```
TransactWriteItems([
  Put: EmailLocks (ConditionExpression: attribute_not_exists(email))
  Put: UserMappings (ConditionExpression: attribute_not_exists(appUserId))
])
```

Chống concurrent signup cùng email. TTL 24h cho lock (phòng orphan).

### 3. Detect federated user

Dùng `event.request.userAttributes.identities` (JSON string), không dùng `userName.includes('_')`.

### 4. JWT claims thay vì fetch API

Inject thẳng vào JWT qua PreTokenGeneration — 5 claims:

- `custom:appUserId`
- `custom:emailSub`
- `custom:googleSub`
- `custom:displayName`
- `custom:email`

Zero network call khi mở Profile modal. Amplify `fetchAuthSession()` đọc từ local cache.

### 5. Mỗi Lambda query GSI duy nhất 1 lần

- `pre-token-generation`: GSI query để lấy `appUserId` lần đầu → inject JWT
- Business APIs (auth-status): đọc `custom:appUserId` từ verified JWT, `GetItem` trực tiếp
- Không redundant query

### 6. Chỉ PreTokenGeneration cần query GSI

PreTokenGeneration query `emailSub-index` → `googleSub-index` để resolve `custom:appUserId` lần đầu.
Mọi API khác (auth-status, set-password) dùng `custom:appUserId` từ JWT claims đã verified.

### 7. PostConfirmation creation vs PreTokenGeneration creation (pending)

| | PostConfirmation | PreTokenGeneration |
|---|---|---|
| Thời điểm | Sau confirm OTP | Lúc login lần đầu |
| UNCONFIRMED | Không có record | Không có record |
| CONFIRMED chưa login | Có record | Không có record |
| Spam email | Record vô dụng tồn tại | Chưa có record |

**Đã thảo luận, chưa implement.** PreTokenGeneration creation được ưu tiên — tránh spam.

### 8. Amplify không support attribute `name`

`defineAuth({ userAttributes })` type `UserAttributes` không có `name`. Buộc giữ `preferredUsername` với label "Display Name" trong frontend.

---

## Files Modified

### Backend — Amplify

| File | Action |
|------|--------|
| `auth/resource.ts` | Removed `profilePicture` + `preferredUsername` from `attributeMapping` and `userAttributes`; kept `preferredUsername: 'email'` in Google mapping (required attr constraint) |
| `backend.ts` | PK `cognitoSub` → `appUserId`; added `EmailLocks` + `emailSub-index`/`googleSub-index` GSIs; updated IAM perms: removed `ListUsers`/`AdminGetUser`, added `TransactWriteItems` + DynamoDB Query for pre-sign-up |
| `functions/post-confirmation/handler.ts` | Full rewrite: `TransactWriteItems` + `EmailLocks` lock; `displayName` = `given_name+family_name` (Google) or `preferred_username` (Email); `emailSub`/`googleSub` split; race retry |
| `functions/pre-sign-up-handler/handler.ts` | Query DynamoDB `email-index` thay vì Cognito `ListUsers`; removed `userName` parsing + `preferred_username` override; error `EMAIL_EXISTS_USE_GOOGLE` |
| `functions/pre-token-generation/handler.ts` | Query `emailSub-index` → `googleSub-index`; inject 5 `custom:*` claims; throw `USER_MAPPING_NOT_FOUND` if no record; no fallback |
| `functions/auth-status/handler.ts` | DynamoDB `GetItem` by `custom:appUserId`; **no Cognito** `AdminGetUser`; no `identities`/`userStatus` |

### Frontend — React

| File | Action |
|------|--------|
| `ProfileContent.tsx` | Read from JWT claims (zero network); Security section with button "Set my Password"; Account Linked uses `googleSub` from JWT; Display Name from `displayName` claim; added `security` nav item |
| `panels/SettingsPanel.tsx` | Stripped: removed auth-status fetch, password states, Set Password form, Google Linked badge. Kept: Profile/Settings nav, Learn More, Billing, Sign Out |
| `AuthGuard.tsx` | Signup field: `'Full Name'` → `'Display Name'`; error key `EMAIL_EXISTS_USE_GOOGLE` |

---

## Pending — Not Yet Implemented

### 1. Set Password — Button handler

**Backend** (`functions/set-password/handler.ts`):
- Sau `AdminSetUserPassword`, Query DynamoDB + UpdateItem `SET emailSub = cognitoSub`
- `ConditionExpression: attribute_not_exists(emailSub)`
- Inject `USER_MAPPINGS_TABLE_NAME` env var + IAM cho set-password Lambda

**Frontend** (`ProfileContent.tsx`):
- Toggle password form (show/hide)
- 2 inputs + Save button
- POST `/api/user/set-password`
- Success → `fetchAuthSession({ forceRefresh: true })` → JWT mới có `emailSub`

### 2. Link Google — Button handler

**Frontend only** (`ProfileContent.tsx`):
- Button "Link" → `signInWithRedirect({ provider: 'Google', options: { prompt: 'select_account' } })`
- Hub listener: `await fetchAuthSession({ forceRefresh: true })` sau `signedIn` event

### 3. PreTokenGeneration creation (thay PostConfirmation)

`post-confirmation/handler.ts`:
- Email chưa có trong DB → return event (no-op)
- Email đã có → link provider (update emailSub/googleSub)

`pre-token-generation/handler.ts`:
- Record không tìm thấy → tạo mới (giống logic creation hiện tại của PostConfirmation)
- Sau đó inject claims

### 4. Force refresh Hub listener

`FloatingNavBar.tsx` hoặc AuthContext:
```typescript
Hub.listen('auth', async ({ payload }) => {
  if (payload.event === 'signedIn') {
    await fetchAuthSession({ forceRefresh: true })
  }
})
```

---

## Lessons Learned

### `TransactWriteItems` IAM

CDK `grantReadWriteData` **không** bao gồm `dynamodb:TransactWriteItems`. Cần thêm thủ công.

### `IFunction` vs `Function` cast

`backend.authStatusHandler.resources.lambda` typed as `IFunction` — không có `.addEnvironment()`. Cần cast `as Function`.

### 3 Lambda trigger permissions

Cognito trigger Lambdas cần 2 permissions:
1. `addPermission` (Resource policy — Cognito được invoke Lambda)
2. `addToRolePolicy` (Execution role — Lambda gọi được service khác)

### Amplify `userAttributes` type limit

`defineAuth({ userAttributes })` không support `name`. Fallback: `preferredUsername` + rename label.

### JWT claims injection

PreTokenGeneration inject claim `custom:appUserId` (cần `custom:` prefix) để tránh conflict với Cognito reserved claims. Business APIs đọc từ verified JWT claims.

### Chi phí

DynamoDB `GetItem` ≈ $0.0000005/request. Inject claim vào JWT = $0 (no network). Tối ưu hơn fetch API Gateway mỗi lần mở modal.

---

## Test Results

| Check | Status |
|-------|--------|
| TypeScript — `frontend/` | ✅ Pass (0 errors) |
| TypeScript — `amplify/` | ✅ Pass (0 errors) |
| ESLint (changed files) | ✅ Pre-existing `any` types only |

E2E tests pending: cần `npx ampx sandbox deploy` + manual test.

---

## Archival: features removed from SettingsPanel

- **Set Password form** — chuyển về ProfileContent Security section (TBD handler)
- **Google Linked badge** — chuyển về ProfileContent Account Linked section
- **auth-status fetch** — không còn gọi API, dùng JWT claims
