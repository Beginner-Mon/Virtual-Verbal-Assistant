# 31-08-2026 — Avatar Background Customization (UI-only)

> Branch: `feature/langgraph-rewrite` | Plan: `docs/plans/avatar-bg-customization-plan.md`

## Summary
Implemented UI-only avatar background color picker. No backend/DB/Cognito changes. Avatar now purely color + logo (removed Google `picture` usage). Edit badge + nested modal syncs across FloatingNavBar, MobileNavBar, ProfileContent via React Context + localStorage.

## Changes

### New files
- `ECA_UI/frontend/src/lib/avatarPalette.ts` — 8 colors `slate/violet/blue/emerald/amber/rose/cyan/indigo` with `AvatarBgId`
- `ECA_UI/frontend/src/contexts/AvatarBgContext.tsx` — `AvatarBgProvider` + `useAvatarBg()`, persists `vva_avatar_bg` in localStorage, global sync
- `ECA_UI/frontend/src/components/AvatarPickerModal.tsx` — fixed `z-[10001]` overlay, preview `AvatarWithLogo size="lg"` on top, 4x2 color grid below, Check on selected, Escape/overlay close

### Modified files
- `ECA_UI/frontend/src/components/AvatarWithLogo.tsx` — **BREAKING UI**: removed `profilePicture?: string` prop and `<AvatarImage>`, now only `<AvatarFallback>` with `bgClassName?: string` + `logoClassName?: string` via `cn(bgClassName ?? 'bg-muted-foreground/20', logoClassName)`. Deleted `AvatarImage` import. Logo fill now `currentColor` driven by `logoClassName`.
- `ECA_UI/frontend/src/lib/avatarPalette.ts` — added `logoClassName` per entry: `slate` -> `text-muted-foreground` (xám nhạt nền -> logo xám đậm), `violet` -> `text-violet-100`, `blue` -> `text-sky-100`, `emerald` -> `text-emerald-100`, `amber` -> `text-amber-950` (nền vàng nhạt -> logo nâu đậm), `rose` -> `text-rose-100`, `cyan` -> `text-cyan-50`, `indigo` -> `text-indigo-100`. Dark bg -> light logo tint, light bg -> dark logo — giống logo gốc.
- `ECA_UI/frontend/src/layouts/MainLayout.tsx` — wrapped content with `<AvatarBgProvider>` (inside `GraphicsProvider`, outside `<main>`), so FloatingNavBar/MobileNavBar/ProfileContent share one color state
- `ECA_UI/frontend/src/components/ProfileContent.tsx` — added `Pen` icon button `w-6 h-6 rounded-full absolute -right-1 -bottom-1 cursor-pointer` + `Pen w-3 h-3` (nhỏ lại, thêm `cursor-pointer`), `useAvatarBg()` + `showPicker` state, `<AvatarWithLogo bgClassName={bg.className} logoClassName={bg.logoClassName}>`, render `AvatarPickerModal` on click
- `ECA_UI/frontend/src/components/FloatingNavBar.tsx` — replaced `useAuth().userAttributes.picture` + `profilePicture` with `useAvatarBg().bg`, `<AvatarWithLogo size="sm" bgClassName={bg.className} logoClassName={bg.logoClassName}>`
- `ECA_UI/frontend/src/components/MobileNavBar.tsx` — same replacement for `size="xs"`
- `ECA_UI/frontend/src/components/AvatarPickerModal.tsx` — preview now passes `logoClassName`, grid options `cursor-pointer`, `Check` color adapts (`slate` -> `text-muted-foreground`, `amber` -> `text-amber-950`, others `text-white`)

## Fix: giữ màu gốc xám nhạt
- Ban đầu `AvatarWithLogo` fallback là `bg-muted-foreground/20` (xám nhạt). Sau khi thêm `AvatarBgContext` default `slate` = `bg-slate-500` (#64748b) nên màu gốc bị đổi thành xám đậm — báo bởi Owner.
- Fix: `lib/avatarPalette.ts` entry `slate` đổi `bg-slate-500` -> `bg-muted-foreground/20` để default trùng gốc. `AvatarWithLogo.tsx` đổi `cn('bg-muted-foreground/20', bgClassName)` -> `cn(bgClassName ?? 'bg-muted-foreground/20')` để không stack 2 bg class.
- `npx tsc --noEmit` + `vite build` re-verify pass. Reload hiện lại xám nhạt như cũ; pick màu khác vẫn đổi toàn app.

## Fix 2: nhỏ icon edit + logo 2 màu (31-08-2026 post-review)
- `ProfileContent.tsx` badge `w-7 h-7` + `Pen w-3.5` -> `w-6 h-6` + `Pen w-3 h-3`, thêm `cursor-pointer` theo yêu cầu "nhỏ lại, thêm cursor pointer".
- Avatar 2 màu: `avatarPalette.ts` thêm `logoClassName` cho mỗi bg. `AvatarWithLogo.tsx` nhận thêm `logoClassName` và áp vào `AvatarFallback` (fill `currentColor`). Quy tắc: nền đậm -> logo nhạt cùng hue (violet-500 -> violet-100, sky-500 -> sky-100...), nền nhạt (slate `bg-muted-foreground/20` -> `text-muted-foreground`, amber-500 -> `text-amber-950`).
- `AvatarPickerModal` preview + grid `Check` cũng đổi màu theo bg để đủ contrast.

## CharactersPanel: đổi màu khung theo hex mới (31-08-2026)
- `AvatarsPanel.tsx`: thêm `CHARACTER_BG_OVERRIDES = { anne: '#AAA4C8', bronya: '#A9D8E8', miki: '#39C5BB' }`, `hatsune-miku` giữ mesh emerald cũ (`meshStyle` hash). `CHARACTER_LOGO_COLORS = { anne: '#2d3748', bronya: '#2c5282', miki: '#ffffff', 'hatsune-miku': '#ffffff' }` — nền nhạt -> logo đậm, nền đậm/vừa -> logo trắng, giống logic avatar (không tô đen logo bừa).
- Thay `meshStyle(slug)` -> `characterBgStyle(slug)` (nếu có override thì `backgroundColor` solid hex, không thì `meshStyle` hash). Thay `<img src="/eca-logo.svg" filter invert>` -> `<EcaLogo>` với `style={{ color: characterLogoColor(slug) }}` để logo đổi màu theo nền.
- Kết quả: Anne #AAA4C8 (tím nhạt) + logo đậm, Bronya #A9D8E8 (xanh nhạt) + logo đậm, Miku giữ xanh lá emerald + logo trắng, Miki #39C5BB teal + logo trắng — đúng ảnh và đúng thứ tự Owner yêu cầu.

## Verification
- `npx tsc --noEmit` — pass (no output)
- `npm run build` — pass (vite 6.4.3, 3481 modules, ~7s, no type errors)
- Manual: change color in ProfileContent picker -> FloatingNavBar + MobileNavBar update instantly via Context, reload keeps via localStorage

## Notes
- `AvatarImage` still exists in `ui/avatar.tsx` but no longer used by `AvatarWithLogo` (kept for other potential uses)
- `AuthContext` still exposes `picture` but avatar UI no longer reads it — per Owner request "không còn dùng ảnh từ google"
- Plan scope was "tới modal skeleton" — no Save/Cancel, select = immediate apply. Next phase will extend modal details.

## Next
- Owner to review modal visuals (Pen badge position, palette)
- Phase 2 (if needed): persist to backend or add more options in `AvatarPickerModal`
