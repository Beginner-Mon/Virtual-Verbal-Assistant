---
date: 2026-08-10
tags: [worklog, refactor, dependencies, env, config, decoupling]
author: K
branch: feature/langgraph-rewrite
---

# Worklog 10-08-2026 — K

Cắt phụ thuộc của service LangGraph vào `agentic_rag_gemini`. **Chưa xoá gì** —
theo đúng yêu cầu N: cắt trước, chạy ổn rồi mới xoá.

Kết quả kiểm chứng: `pytest tests/langgraph_agents -m unit` → **281 passed, 0 failed**
(trước: 270 passed / 8 failed).

---

## 0. Phát hiện trước khi sửa

### `requirements-langgraph.txt` thiếu 5 package

| Thiếu | Import ở |
| --- | --- |
| `langchain-google-genai` | `llm.py` |
| `sentence-transformers` | `shared/embedding.py`, `core/resource_guard.py` |
| `numpy` | `shared/embedding.py` |
| `PyYAML` | 6 file |
| `SQLAlchemy` | `alembic/env.py` |

Service chạy được **chỉ vì** `agentic_rag_gemini/requirements.txt` đã cài chung
env. Ai cài theo file này trên máy sạch — hoặc build Docker image cho Phase 7 —
thì backend chết ngay lúc import.

### 8 test đỏ hôm 08/08 KHÔNG phải do thiếu dependency

Tôi ghi sai trong worklog 08/08. Nguyên nhân thật: **`pip` và `python` trỏ vào hai
môi trường khác nhau.**

```
pip    -> C:\Users\Nguyen\miniconda3\envs\firstconda\Lib\site-packages
python -> C:\Users\Nguyen\AppData\Local\Programs\Python\Python312\python.exe
```

`pip show langchain-google-genai` báo có (4.2.3) trong khi
`python -c "import langchain_google_genai"` báo không — cùng lúc, không mâu thuẫn,
vì đó là hai interpreter. Chạy lại suite bằng `firstconda`: **0 test đỏ**, kể cả 2
test planner mà tôi đã quy cho "ô nhiễm thứ tự test".

> Cả `docs/ops/runbook.md` lẫn `scripts/QUICKSTART.md` đều viết `python -m uvicorn`
> trần. Trên máy này lệnh đó chạy Python312 — thiếu `langchain_google_genai`, mà
> import của nó là **lazy** nên backend vẫn khởi động bình thường và chỉ chết lúc
> Gemini fallback được gọi, tức đúng lúc DeepSeek đang hỏng. Ghi vào tech-debt.

### 4 loader `.env`, 4 thứ tự khác nhau

`llm.py`, `db/postgres.py`, `local_tests/run_ollama_node_smoke.py`,
`db/migrations/migrate_messages.py`. File nào thắng phụ thuộc **module nào được
import trước** — và ca thua thì im lặng: không `VVA_PG_DSN` ⇒ app rơi về Postgres
local trong khi migration chạy trên Neon.

`local_tests` còn load **mọi** file khớp thay vì dừng ở file đầu.

---

## 1. Một loader duy nhất — `shared/env.py`

Canonical: **`agenticRAG/.env`**. Vẫn chấp nhận `agentic_rag_gemini/.env` nhưng
**log WARNING** kèm hướng dẫn di chuyển — để backend đang chạy không gãy ngay,
mà cũng không quên được.

Biến môi trường thật **luôn thắng** (`override=False`): container/systemd truyền
config là setup đúng, một file `.env` sót trên image không được phép đè.

Không tìm thấy file thì **warn, không raise** — Docker/CI cấp config qua biến môi
trường là hợp lệ, raise sẽ giết đúng những setup đúng. Chỗ **phải** kêu to là chỗ
gây thiệt hại, và giờ nó kêu:

```
No VVA_PG_DSN / POSTGRES_DSN set and no dsn in langgraph.yaml — falling back to
the LOCAL database (...). Configuration source: ... If you meant to use the
managed database, this is the bug.
```

Trước đây dòng này **hoàn toàn im lặng**.

### `.env` mới

Tạo `agenticRAG/.env` từ file cũ, **bỏ 13 biến chết** (di sản pipeline RAG cũ —
`TOP_K_RETRIEVAL`, `SIMILARITY_THRESHOLD`, `ENABLE_SUMMARIZATION`…). Giữ 11 biến.
Verify DSN host khớp y hệt bản cũ (`ep-lingering-salad-a1qixkrs…neon.tech`).

**File cũ vẫn còn nguyên** — `.env` bị gitignore nên **git không khôi phục được**,
copy chứ không move. Xoá sau khi N xác nhận.

Thêm `agenticRAG/.env.example` (được commit) — tài liệu đầy đủ 26 biến service
đọc, kèm default, không chứa secret nào.

---

## 2. `migrate_messages.py` trỏ nhầm database

Không chỉ load `.env` sai chỗ — nó đọc `DATABASE_URL` (**không nơi nào set**) với
default `postgresql://postgres:postgres@localhost:5432/vva`: khác **cổng** (5432 vs
5433), khác **user** (postgres vs vva), khác cả hai database đang dùng.

Chạy nó ra thì hoặc không kết nối được, hoặc migrate nhầm một database khác.

Sửa: dùng `get_default_dsn()` mới trong `db/postgres.py` — script và migration giờ
resolve database **y hệt cách app resolve**.

---

## 3. Ba script trỏ vào đường dẫn không tồn tại

| Script | Trỏ vào | Thực tế |
| --- | --- | --- |
| `cleanup_tts_audio.py` | `agentic_rag_gemini/langgraph_agents/services/vieneu_tts/outputs` | **Chưa từng tồn tại.** `langgraph_agents` là anh em của `agentic_rag_gemini`, không phải con |
| `spike_astream.py` | `sys.path` sâu hơn một cấp | Import không thể resolve — spike chưa từng chạy được |
| `run_stack.py` | `agentic_rag_gemini` | **Đúng** — nó khởi động stack cũ. Không được "sửa" |

`cleanup_tts_audio.py` là đáng ngại nhất: nó in một dòng rồi **exit 0**. Nếu ai đó
đã cắm nó vào Task Scheduler thì suốt nhiều tháng nó báo thành công 30 phút một
lần mà không xoá gì. Kiểm tra thực tế: **86 file WAV** đang nằm ở
`SpeechLLm/data/temp_audio` — nơi TTS thật sự ghi.

Sửa: trỏ đúng chỗ, thiếu thư mục thì **exit 1** (kèm `raise SystemExit(main())` —
trước đó `main()` trả về status mà không ai truyền ra). Tiện thể sửa
`freed // 1024 // 1024:.1f` (chia nguyên trước ⇒ dưới 1 MB luôn hiện "0.0 MB").

`run_stack.py` + `docs/ops/setup-guide.md`: gắn banner DEPRECATED. Setup-guide vẫn
đang bảo người ta chạy `run_stack.py` + ChromaDB + frontend :3000 — dựng lên một
stack song song đọc database khác.

---

## 4. KB corpus ra khỏi package cũ

`documents.txt` (2918 bài tập, 1.1 MB) chỉ nằm trong `agentic_rag_gemini/data/` —
tức knowledge base của hệ thống **hiện tại** không rebuild được nếu không có thư
mục sắp bị xoá. Copy sang `data/knowledge_base/`, sha256 khớp byte-for-byte.

`ingest_kb_pgvector.py` giờ `resolve_source()`: canonical trước, legacy sau kèm
NOTE. Verify: resolve đúng đường mới, parse **2918/2918**, guard backend vẫn chặn
(exit 2).

> ⚠️ **Cần N quyết**: `data/` ở gốc repo **không** bị gitignore (rule `data/` nằm
> trong `.gitignore` riêng của `agentic_rag_gemini`). Nên file 1.1 MB này sẽ hiện
> ra là untracked. Commit vào git hay thêm vào `.gitignore`? Hiện corpus **chỉ tồn
> tại trên máy N** — mất ổ là mất.

---

## 5. Test chống trôi — `test_requirements_complete.py`

3 test, parse AST của toàn bộ `langgraph_agents`:

1. `test_every_import_is_declared` — mọi import third-party phải có trong
   `requirements-langgraph.txt`
2. `test_no_legacy_imports` — không được import `agentic_rag_gemini` (tách riêng để
   thông báo lỗi nói đúng vấn đề, thay vì bảo "thêm agentic_rag_gemini vào requirements")
3. `test_requirements_file_parses_to_something_sane` — canh chính cái canh: parser
   hỏng thì mọi thứ pass

Kiểm tra **tĩnh**, không phải runtime — vì môi trường chạy test cũng chính là môi
trường có sẵn các package thừa, nên kiểm tra runtime sẽ không bao giờ bắt được lỗi
này.

**Mutation check**: xoá `PyYAML` khỏi requirements → đỏ đúng 1 test, kèm tên file
đầu tiên import `yaml`.

Bắt được luôn một lỗi trong chính bản quét tay của tôi hôm trước: **4 file có
BOM** (`__init__.py` × 3 + `vieneu_tts/__init__.py`), bản quét cũ nuốt
`SyntaxError` và bỏ qua chúng. Test dùng `utf-8-sig`.

---

## 6. Thử thật — chạy service khi `agentic_rag_gemini` KHÔNG tồn tại

Test tĩnh chứng minh không còn `import`. Nó **không** chứng minh không còn đường
dẫn nào trỏ vào đó lúc chạy. Nên đổi tên cả thư mục đi rồi chạy lại từ đầu:

```
mv agenticRAG/agentic_rag_gemini agenticRAG/_HIDDEN_agentic_rag_gemini
```

Lưu ý: thao tác này làm **`.env` cũ biến mất theo** — tức là bài test cũng kiểm
luôn việc `.env` mới đã thật sự thay thế được nó.

| Kiểm | Kết quả |
| --- | --- |
| `pytest tests/langgraph_agents -m unit` | **281 passed, 0 failed** |
| Uvicorn khởi động (:8001) | `startup_complete`, `graph_loaded: true` |
| **MCP discovery** | `mcp_discovery_ok`, **2 tool / 2 server** |
| `/health/detailed` | `status: ready`, HTTP 200, **7/7 check ok** |
| `POST /chat` (SSE, tiếng Việt) | 7 stage, 812 token event, **1834 ký tự** |
| Retrieval | trả về nguồn `exercise_db` ⇒ pgvector KB hoạt động |

Stage đi đủ chuỗi: `memory → planner → retriever_agent ×2 → synthesizer ×2 → grader`.

MCP là mục đáng lo nhất trước khi thử, vì `mcp/client.py` từng bơm `PYTHONPATH`
trỏ vào package cũ. Comment còn nói vậy nhưng code thì không — và
`mcp_discovery_ok 2 tool` xác nhận điều đó bằng thực nghiệm chứ không bằng đọc code.

Khôi phục xong: thư mục về chỗ cũ, `.env` cũ còn nguyên (1898 byte), `git status`
sạch, backend gốc :8000 vẫn 200, chạy lại pytest vẫn **281 passed**.

> Ghi lại vì mất thời gian: `/chat` trả về **SSE**, không phải JSON. Và curl trên
> Git Bash làm hỏng tiếng Việt trong `-d` (HTTP 400 "error parsing the body") —
> phải ghi payload ra file UTF-8 rồi `--data-binary @file`. Trường là `query`,
> không phải `message`.

---

## Còn lại

- **Chưa xoá `agentic_rag_gemini`** — đúng kế hoạch, nhưng §6 đã chứng minh xoá
  được. Khi xoá thì đi kèm: `run_stack.py`, phần legacy trong `setup-guide.md`,
  `tests/agenticRAG/` (test của package cũ, **chưa** chạy trong bài thử §6).
- **Chưa chứng minh trên venv sạch.** Cài đủ `requirements-langgraph.txt` kéo theo
  torch ~2 GB. Test tĩnh chứng minh *danh sách đủ*, không chứng minh *cài được*.
- `.env` cũ và `documents.txt` cũ vẫn còn — cố ý.
- **`python` trần vẫn là bẫy.** Runbook/QUICKSTART chưa sửa. Nên đổi thành đường dẫn
  tuyệt đối tới `firstconda`, hoặc thêm check interpreter lúc khởi động.
