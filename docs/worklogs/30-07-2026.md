---
date: 2026-07-30
tags: [worklog, frontend, animation, fsm, kb, pgvector]
author: K
branch: feature/langgraph-rewrite
---

# Worklog 30-07-2026 — K

Ba khối việc: **P0 KB ingest**, **sửa plan FSM (9 lỗi)**, **implement FSM refactor**.

## 1. P0 — KB ingest vào pgvector (xong)

Root cause của bug "câu hỏi PT đơn giản cũng bị refuse" (Owner báo hôm trước) **không phải grader** —
bảng `kb_embeddings` **rỗng hoàn toàn**. Hai script ingest legacy (`ingest_medquad.py`,
`ingest_humanml3d.py`) đều target ChromaDB của kiến trúc cũ.

- 🆕 `scripts/ingest_kb_pgvector.py` — parse `documents.txt` (2918 record, phân cách `---`), bỏ
  description `"nan"` (pandas NaN lọt export), inline structured field vào content để body-part/
  equipment query có bề mặt semantic để match, `embed_passages` batch 64, `--reset` idempotent theo
  `source_type='exercise_db'`.
- 🐛 **Bug phát hiện thêm**: migration `002` tạo HNSW cho `summaries.embedding` nhưng **bỏ sót**
  `kb_embeddings.embedding` → mọi `kb_search` là seq scan. Thêm `003_kb_embeddings_hnsw.py`
  (`vector_cosine_ops`, khớp toán tử `<=>`). Verify planner dùng thật:
  `Index Scan using idx_kb_emb_embedding`.
- Kết quả: `kb_embeddings` **0 → 2918**. `kb_search` trả 5 result (trước 0), sim 0.92 EN / 0.82 VI.
  Pipeline thật: `mode: refuse` → **`mode: synthesize`**.
- ⚠️ **Chưa quyết**: corpus là **gym/fitness**, không phải PT lâm sàng; 53% record (1550/2918)
  không có description. Đã fix lỗi "KB rỗng", **chưa** làm nó thành KB lâm sàng. Owner gác lại.

## 2. Sửa plan `animation-fsm-refactor.md` → v1.2 (9 lỗi)

Owner yêu cầu ưu tiên sửa lỗi trong plan trước khi implement. Hai lỗi mới tìm được trong phiên này:

- 🔴 **#8 Boot sequence sai.** Plan ghi `Khởi tạo → transitionTo('idle')`, nhưng code thật boot vào
  `action_greeting` **rồi mới** về idle (`MotionContext.tsx:93-97` ưu tiên `action_greeting` làm
  `defaultMotionId`; URL chứa `action_` → `isAction` → LoopOnce → `finished` → idle). Implement đúng
  plan = **lặng lẽ xoá lời chào**, không test nào bắt được. → thêm **§2.5** với 3 điều kiện bắt buộc:
  fallback `idle` khi clip greeting fail (không có thì `currentAction = null` → **T-pose**), latch
  `hasGreeted` để không chào lại khi đổi model/StrictMode, và không chặn tương tác trong lúc chào.
- ⚠️ **#9 Chi phí bảo trì.** Owner hỏi thẳng: "thêm animation về sau có dễ maintain không?". Đếm ra
  plan có **6 map song song** cùng khoá `CharState` → thêm 1 animation phải sửa **4-7 nơi**, trong đó
  **3 nơi không có compile check** (`ALLOWED_TRANSITIONS` reachability viết tay, `ON_FINISHED` là
  `Partial<>`, `STATIC_ENTRIES` khai `Record<string,…>`). Đó **đúng error class đã sinh ra 2 lỗi 🔴
  của v1.0** → refactor mà giữ nó thì chỉ đổi hình dạng lỗi. → thêm **§3.0** (một bảng `STATES`,
  reachability thành hàm) và **§11** (cookbook + giới hạn thật).

Cũng sửa lỗi tầng ở §9.4 theo Owner: emotion là **data** trong `profiles/*.ts`, không phải API —
bỏ hẳn đề xuất "thêm method vào `AvatarController`", chuyển policy vào trường `facial` của `STATES`.

## 3. Implement FSM refactor (xong, 17/17 test xanh)

### Files

| File | | |
|---|---|---|
| `lib/AnimationStates.ts` | 🆕 | Bảng `STATES` (7 state) + `canTransition` + derived getters |
| `lib/motionAssets.ts` | 🆕 | Nơi duy nhất glob file animation (ngoài plan — tránh 2 nguồn sự thật) |
| `lib/AnimationRegistry.ts` | 🆕 | Load → retarget → subclip → cache, per-VRM |
| `lib/AnimationController.ts` | 🆕 | FSM runtime, giữ invariant T-pose |
| `lib/CameraController.ts` | 🆕 | Mode + cooldown 3s, **không** lái FSM |
| `hooks/useFsmTriggers.ts` | 🆕 | `useFsmBoot`, `useAutoAfterTrigger` |
| `contexts/MotionContext.tsx` | ♻️ | Bỏ `selectedMotionId`/`isThinking`/3 effect string-match |
| `components/CharacterViewer.tsx` | ♻️ | Bỏ ~95 dòng clip-loading/fade/subclip/`isAction` |
| `components/panels/MotionControlPanel.tsx` | ♻️ | 2 dropdown: state (derived) + file debug |
| `components/ChatPanel.tsx` | ♻️ | `setIsThinking` → `transitionTo` + guard `thinkingRef` |

**Cả 7 chỗ string-match ở §0 của plan đã biến mất.** Không còn nơi nào quyết định hành vi animation
dựa trên tên file.

### Điểm code khác plan (chi tiết + lý do ở plan §12.2)

Đáng chú ý: `StaticSource` thành discriminated union theo `loader` (plan cho phép tổ hợp vô nghĩa
`isFbx:true` + `retarget:'smplx'`); `autoAfter` có `to` tường minh thay vì "state from-idle đầu
tiên" (magic theo thứ tự khai báo); `ON_FINISHED` hop nằm **trong** controller (để ngoài thì mọi
call-site đều có thể quên → kẹt frame cuối); `constructor(private x)` phải bỏ vì tsconfig bật
`erasableSyntaxOnly`.

### Test — Playwright, `:5173` + backend `:8000`

| # | Đo được |
|---|---|
| 1 Deadlock regression | `exercise` → finished → `idle`; `transitionTo('thinking_intro')` = **true** |
| 2 Motion lúc thinking | `thinking_loop` → `exercise` OK |
| 3 T-pose invariant | 10 transition/1s, không await → **0 frame mất pose** |
| 4 Kimodo retarget | generated BVH chạy qua `exercise` OK |
| 5 `reset()` fix | cùng clip 2 lần: 5483ms / **5213ms** (kẹt frame cuối sẽ ≈0) |
| 6 Camera cooldown | head → hips → (idle +0.9s) **hips** → +3.5s head |
| 7 `invalidate()` | đổi model → motion phát lại đúng, không méo |
| 8 Chat end-to-end | `["thinking_intro","thinking_loop","thinking_outro"]` → `idle` sau 18.8s |
| 9 Boot | pose đầu tiên = **`greeting`**, greeting đúng **1 lần** (StrictMode OK) |
| 10 Đổi model | **không** chào lại, về `idle` |

Thêm 5 test ngoài checklist: reachability guard (3 transition sai đều `false`, `thinking→exercise`
`true`), state dropdown derived 4 mục, file dropdown vẫn có 3 file generated, dropdown lái được FSM,
file selector → `exercise` + camera `hips`.

`npx tsc --noEmit` xanh, `npm run build` xanh.

Thêm `window.__fsm` (chỉ DEV) ở `MotionProvider` — phiên trước mất thời gian vì `window.__avatar` chỉ
tồn tại khi panel mở; handle này luôn có nên test tự động không phụ thuộc UI.

## 4. Bug "UI chớp đen" (Owner báo sau khi merge FSM)

> **Đọc cả mục.** Lần chẩn đoán đầu (shadow ping-pong) tìm ra một lỗi thật nhưng **không phải**
> nguyên nhân Owner nhìn thấy — Owner báo lại "vẫn còn chớp tắt". Root cause thật là **Bloom**, ở
> tiểu mục cuối.

Owner báo màn hình đôi lúc **chớp đen rất nhanh**, tần suất **tăng nhẹ** sau code FSM.

### Lần 1 (chưa trúng): ping-pong three.js ↔ R3F, mỗi React re-render = rebuild toàn bộ shadow map

`ENV_CONFIG.shadows.type` là `THREE.PCFSoftShadowMap` — **hằng số đã deprecated**. Chuỗi nhân quả:

1. `three/build/three.module.js` — `WebGLShadowMap.render()`:
   ```js
   if ( this.type === PCFSoftShadowMap ) {
     warn('WebGLShadowMap: PCFSoftShadowMap has been deprecated. Using PCFShadowMap instead.')
     this.type = PCFShadowMap        // ← MUTATE giá trị của mình
   }
   ```
2. `@react-three/fiber` `configure()` chạy **mỗi lần `<Canvas>` render**, `Object.assign(gl.shadowMap, shadows)`
   ghi `PCFSoftShadowMap` trở lại, rồi:
   ```js
   if (oldEnabled !== gl.shadowMap.enabled || oldType !== gl.shadowMap.type) gl.shadowMap.needsUpdate = true
   ```
   `oldType` = `PCFShadowMap` (three vừa ghi) ≠ `PCFSoftShadowMap` (config) → **`needsUpdate = true`**.
3. → rebuild **toàn bộ** shadow map trong frame đó → frame tối → chớp đen. Rồi three lại downgrade
   → lặp vô hạn ở mỗi re-render.

**Lỗi này có TRƯỚC refactor.** Refactor làm nó lộ ra nhiều hơn vì `currentState` + `cameraMode` giờ
đẩy qua context → `CharacterViewer` (chủ `<Canvas>`) re-render **mỗi lần đổi state** thay vì chỉ khi
đổi motion. Đúng như Owner mô tả: "tần suất tăng 1 chút".

### Đo trước/sau (đếm warning — nó phát ra **từ trong** `WebGLShadowMap.render()`, tức đúng event rebuild)

| Tình huống | Trước | Sau |
|---|---|---|
| Boot + greeting | 4 rebuild | **0** |
| 4 FSM transition | +4 | **+0** |
| 1 exercise (kèm camera hips→head) | +2 | **+0** |
| Ngồi im 5s | +0 | +0 |

### Fix

1. 🔑 **`environmentConfig.ts`: `PCFSoftShadowMap` → `PCFShadowMap`** — chính là giá trị three.js
   **vẫn đang dùng** (nó tự downgrade), nên **hình ảnh không đổi**, chỉ khác là không còn ping-pong.
   Một dòng này xoá hẳn triệu chứng, độc lập với số lần React re-render.
2. `CharacterViewer.tsx`: hoist `gl` / `camera` / `shadows` của `<Canvas>` ra **module const** — trước
   đó là object literal inline nên `configure()` nhận object mới mỗi render.
3. `CharacterViewer.tsx`: `new THREE.AxesHelper(3)` mỗi render → `useMemo` (R3F đang detach/dispose
   rồi attach lại object mỗi lần re-render).
4. `ScenePostProcessing.tsx`: `memo()` — component không nhận prop, chỉ đọc config tĩnh.
5. `MotionContext.tsx`: `useMemo` cho context value — trước đó object literal inline = value mới mỗi
   lần provider render → mọi consumer re-render kể cả khi dữ liệu của nó không đổi.

Verify sau fix: **11/11 + chat test vẫn xanh**, `tsc` + `build` xanh, avatar render bình thường
(ảnh scratchpad `shot2.png`).

> ⚠️ Có một lần screenshot ra canvas trắng trơn giữa lúc fix — **không phải regression**: ảnh đó logo
> ECA cũng là broken-image, tức Vite dev server đang reload sau khi tôi sửa file. Load lại là bình
> thường, và full suite chạy lại xanh hết.

### ⚠️ Fix trên KHÔNG dứt điểm — Owner báo lại "vẫn còn chớp tắt"

Fix shadow ping-pong là **thật** (rebuild 4→0) nhưng **không phải nguyên nhân** Owner nhìn thấy.
Phải đo lại bằng pixel thật thay vì proxy.

#### Vì sao 2 lần đo đầu vô nghĩa

- **Headless Chromium không chạy compositor thật**: `readPixels`/`drawImage` từ canvas WebGL trả về
  rỗng; CDP screencast chỉ ra **39 frame/20s**. Mọi kết luận "0 frame đen" từ headless là vô giá trị.
- Probe tự viết cũng sai: tôi tô nền magenta rồi `drawImage` — luma 170 chính là **magenta**
  (255+0+255)/3, tức "không vẽ được gì", không phải "màn hình tối".
- Chạy lại **headed** + `--disable-backgrounding-occluded-windows` (+ `bringToFront`) mới có
  ~59fps thật. Cũng chính vì rAF bị throttle mà FSM đứng ở `greeting` mãi → 2 lần test timeout.

#### Đo đúng: CDP screencast headed, 1148 frame/19.5s, đếm frame có luma < 75% median

Frame đen thật đọc **luma 17.8** giữa các frame bình thường **~220** — tức **đúng 1 frame đen đơn lẻ**,
không liên quan sự kiện app nào. Ảnh frame đó: **vùng canvas đen đặc, UI HTML (nav, logo) vẫn bình
thường** → lỗi nằm ở đường render WebGL, không phải DOM.

#### Bisect

| Cấu hình | Frame đen / 19.5s |
|---|---|
| Bật hết | 4 |
| Tắt SSAO | 3 → **không phải SSAO** |
| Tắt ContactShadows | 5 → **không phải ContactShadows** |
| Tắt cả EffectComposer | **0** |
| **Tắt Bloom, giữ nguyên phần còn lại** | **0** ← khoanh trúng |
| Bloom bật lại kèm `mipmapBlur` | 6 → đổi đường blur **không cứu được** |

#### Fix

`ENV_CONFIG.postProcessing.bloom.enabled = false` + `ScenePostProcessing` gate theo cờ đó (dựng mảng
effect thay vì `&&` inline, vì `EffectComposer` type children là element nên `false` là lỗi compile).

Đánh đổi: Bloom đang ở `intensity 0.15` / `threshold 0.85` — gần như không nhìn thấy, trong khi nó
gây **1 frame đen mỗi ~3-5s**. Không phải quyết định khó. Cờ vẫn còn để Owner bật lại; comment trong
config ghi nguyên bảng số đo để lần sau ai bật lại thì biết phải đo lại thế nào.

Verify cuối: **0 frame đen** trên 1148 frame, min luma 215.6 (trước: 17.8). FSM suite **11/11** +
`tsc` + `build` xanh.

#### Thứ khác tìm được trong lúc đo (đã sửa luôn)

Load clip **lần đầu** chặn main thread: `thinking_intro` **111ms**, `bored` **214ms** (warm chỉ
21-40ms) → render loop khựng đúng lúc user vừa hỏi câu đầu tiên. Thêm
`AnimationRegistry.prefetchStatic()` — warm mọi clip tĩnh qua `requestIdleCallback`, mỗi callback 1
clip, gọi sau khi boot xong. Sau fix: max frame gap **35ms** ở mọi phase.

### 🔎 Phát hiện thêm, CHƯA sửa (chờ Owner quyết — nó đổi hình ảnh)

`ScenePostProcessing` bật SSAO nhưng console báo **6 lần**:
`"Please enable the NormalPass in the EffectComposer in order to use SSAO."`
→ SSAO đang **không hoạt động**: `<EffectComposer>` không có `enableNormalPass`. Tức đang trả giá
cấu hình + 1 effect pass mà không nhận được occlusion nào. Hai hướng: bật `enableNormalPass` (tốn
thêm 1 pass, có thể xung đột outline MToon — chính lý do comment trong file cảnh báo), hoặc tắt
`ssao.enabled` cho gọn. Cả hai đều đổi hình ảnh nên không tự quyết.

## 5. Shadow frustum clipping (Owner báo) — sửa bằng auto-fit, không nới `cameraSize`

Owner mô tả: bóng bị chém một đường thẳng, vươn tay ra thì tay mất bóng; và yêu cầu **đừng** nới
`cameraSize` vì vừa nặng vừa mờ.

### Đo trước (probe mới `window.__shadow`, so frustum với 51 bone + 51 điểm bóng đổ xuống sàn)

| | Giá trị |
|---|---|
| Frustum | 5.00 × 5.00, **cố định ở gốc toạ độ (0,0,0)** |
| Subject chiếm | **7-8%** chiều ngang |
| Lề dưới | y = −2.07 vs đáy −2.5 → **0.43** |
| Subject dịch trong 1 clip | **> 1.2** đơn vị |
| Texel | 0.49 cm |

Frustum **không nhỏ — nó đặt sai chỗ**: ~92% shadow map phí cho vùng trống trong khi nhân vật nằm
sát mép. Nới `cameraSize` chỉ phí thêm và làm mờ thêm.

### Fix: `lib/shadowFit.ts` — fit frustum theo subject mỗi frame

- Frustum suy ra từ **skeleton ∪ bóng chiếu xuống sàn**. Chi tiết dễ sai: bóng đổ xuống sàn nằm cách
  nhân vật **~2.3 đơn vị** với góc đèn này — fit theo mỗi thân là **vẫn cắt bóng** dù thân đã lọt.
- Fit **hình cầu**, không phải box trong light space: cầu bất biến khi xoay → frustum không đổi kích
  thước lúc nhân vật quay → không rung mép bóng.
- **Snap tâm frustum theo texel** để mép bóng không bò khi frustum trượt theo nhân vật.
- **Giữ nguyên hướng đèn** (chỉ dời vị trí + target cùng nhau) — đổi hướng sẽ làm NdotL của MToon
  đổi theo khi nhân vật đi lại.
- `light.target` không nằm trong scene graph → phải `updateMatrixWorld()` tay, nếu không shadow
  camera vẫn ngắm chỗ cũ.

### Kết quả (2 model × 3 clip generated × 22 mẫu, `mapSize` **không đổi** = 1024)

| | Frame bị cắt | Texel |
|---|---|---|
| Box cố định 2.5 | 0/22 *(xương)* — **nhưng ảnh cho thấy mesh BỊ chém** | 0.488 cm |
| **Auto-fit** (padding 0.35) | **0/22**, ảnh xác nhận bóng nguyên vẹn | **0.236–0.278 cm** → sắc hơn **~1.8×** |

### Đã TÁI HIỆN được (Owner gửi ảnh, tôi chụp lại được cả 2 trạng thái)

Ảnh A/B ở góc nhìn từ trên xuống (`scratchpad/floor-HEAD.png` vs `floor-FIT.png`):

- **HEAD (box cố định)**: bóng bị **chém một đường thẳng đứng bên phải + cụt phẳng ở đáy** — đúng
  triệu chứng Owner mô tả. Owner còn gặp trường hợp nặng hơn: **mất bóng hoàn toàn** — cùng một
  nguyên nhân, chỉ là hình chiếu bóng đã ra hẳn ngoài box thay vì mới ló ra một nửa.
- **FIT**: bóng nguyên vẹn cả hai tay, không có cạnh thẳng nào.

### ⚠️ Bài học: test bằng xương là SAI, và nó suýt làm tôi kết luận sai

Vòng đo đầu tôi chỉ kiểm **51 khớp xương** → báo "0 frame bị cắt" ở **cả hai** cấu hình, và tôi đã
định báo cáo là "không tái hiện được". Nhưng thứ **đổ bóng là MESH** — váy, tóc, ruy-băng, cánh vươn
xa hơn mọi khớp xương. Nên box cũ *qua* được bài test xương trong khi trên màn hình bóng vẫn bị chém.

Hệ quả cho code: `fitPadding` **không phải trang trí** — nó là thứ bù cho khoảng cách xương → mesh.
Đã nâng **0.18 → 0.35** và ghi rõ lý do trong config.

### Owner báo tiếp: "mất bóng ngay trước khi model revert về vị trí cũ" → tìm ra nguyên nhân THỨ HAI

Owner đặt giả thuyết: shadow camera không đi theo nhân vật ở khúc cuối động tác. **Đo lại thì giả
thuyết này SAI** — nhưng nó dẫn tới một lỗi khác, nằm ở dữ liệu motion chứ không phải hệ bóng.

Quét cả 3 clip generated, mỗi frame, so fit-centre với hips và đếm điểm ra ngoài frustum:

| Clip | Frame có xương **DƯỚI sàn** | Đáy thấp nhất | fit-centre lệch hips | Frame bị cắt |
|---|---|---|---|---|
| `motion_2026…a658` | 0/199 | +0.02 | 0.16–0.50 | **0** |
| `motion_7b4b8d9e` | 0/338 | +0.03 | 0.03–0.24 | **0** |
| **`motion_b28e8284`** | **289/340 (85%)** | **−1.04 m** | 0.01–0.27 | **0** |

→ Shadow camera **có** bám nhân vật (lệch ≤0.5, **0 frame bị cắt** trên cả 3 clip).

→ Nguyên nhân thật của "mất bóng khúc cuối": **`motion_b28e8284-328.bvh` cho nhân vật chìm xuyên qua
mặt sàn tới 1.04 m, suốt 85% clip.** Mặt sàn (z=0) là vật **nhận** bóng; phần thân nằm **dưới** nó
không thể hắt bóng lên mặt trên. Hệ quả đúng 2 triệu chứng Owner mô tả:
- thân cắt ngang mặt phẳng sàn → bóng bị xén theo **giao tuyến thẳng**;
- lúc gần như toàn thân chìm (cuối clip, ngay trước khi snap về idle) → **mất bóng hoàn toàn**.

Ảnh `scratchpad/sunk.png` (minZ = −1.04): chỉ còn thấy đầu, tay, cánh; **nửa dưới biến mất dưới sàn**
và không có bóng nào — trùng khớp ảnh Owner gửi.

**Đây là lỗi DỮ LIỆU MOTION (root height không được ground), không phải lỗi hệ bóng.** Hai clip kia
không hề chạm dưới sàn → cùng hệ bóng đó vẫn cho bóng đầy đủ. Chưa sửa, cần Owner chốt tầng sửa:
converter `scripts/kimodo_npz_to_bvh.py` (sửa tận gốc cho mọi clip tương lai) hay tầng retarget ở
frontend (cứu được asset đã bundle mà không cần chạy lại Kimodo).

> Lưu ý phân biệt với lỗi frustum ở trên: ảnh `floor-HEAD.png` bị cắt theo **góc vuông** (hai cạnh
> thẳng gặp nhau) — đó là góc của hộp frustum. Còn lỗi chìm sàn cắt theo **một** đường thẳng. Hai lỗi
> khác nhau, cùng biểu hiện "đường thẳng tắp".

### Sửa: ground clamp ở FRONTEND (Owner duyệt "làm đi")

Kế hoạch ban đầu của tôi (sửa `kimodo_npz_to_bvh.py` để ground clip) **đo ra là vô dụng** — phải đổi
hướng giữa chừng. Chuỗi đo:

1. Ba file NPZ hoá ra có **2 định dạng khác nhau**: `a658` là Kimodo (`local_rot_mats`+`root_positions`),
   còn `7b4b8d9e` + `b28e8284` là **AMASS/SMPL-X** (`poses`+`trans`, đầu ra thời DART) — converter
   không đọc được. Đã thêm `load_motion()` nhận cả hai (output cho file Kimodo **bit-identical** với
   bản đã commit → refactor không đổi hành vi).
2. Regenerate 2 file AMASS bằng converter này thì **hierarchy khác** bản đang dùng và motion đổi hẳn
   → **hoàn nguyên**, không đụng vào asset đang chạy tốt.
3. Đo cột dọc thật (retarget đọc bằng `swapYandZ` nên **BVH_Z** mới là trục đứng, không phải Y):

   | Clip | `Zpos` delta so với frame 0 |
   |---|---|
   | `motion_7b4b8d9e` | −1.5 cm |
   | `motion_a658` | −1.7 cm |
   | **`motion_b28e8284`** | **−120.7 cm** |

4. `retargetBVHToVRM` **neo frame 0 vào chiều cao hông lúc nghỉ rồi cộng delta**
   ([bvhToVrm.ts:257-265](../../ECA_UI/frontend/src/lib/bvhToVrm.ts#L257-L265)) → **chiều cao tuyệt
   đối trong BVH bị bỏ qua**. Nên ground ở converter **không có tác dụng gì** với app này. Tụt 1.21 m
   từ tư thế đứng ⇒ chân xuống ~1.04 m dưới sàn — **khớp đúng số đo**.

→ Fix đúng chỗ: 🆕 [`lib/groundClamp.ts`](../../ECA_UI/frontend/src/lib/groundClamp.ts) — mỗi frame,
**sau** khi mixer chạy, lấy điểm xương thấp nhất; nếu dưới sàn thì nâng group model lên đúng bằng
phần thiếu. Chỉ **nâng**, không đẩy xuống: nhảy vẫn rời đất, còn ngã thì dừng lại ở mặt sàn thay vì
xuyên qua. Không cần convert lại ⇒ clip Kimodo stream lúc runtime (P2) cũng được bảo vệ.

Đã cân nhắc phương án "tính 1 offset cho mỗi clip lúc load" (giữ nguyên đường cong dọc) nhưng cần FK
toàn clip và vẫn bó tay với clip tụt sâu hơn chiều cao hông — ghi lý do trong header file.

**Kết quả** (2 model × 3 clip, đo mỗi frame):

| Clip | Frame dưới sàn — trước | sau |
|---|---|---|
| `motion_a658` | 0/199 | 0/198 |
| `motion_7b4b8d9e` | 0/338 | 0/337 |
| **`motion_b28e8284`** | **289/340** (sâu nhất −1.04 m) | **0/334** (minZ 0.00) |

Ảnh `scratchpad/grounded.png`: đúng khoảnh khắc trước đây minZ = −1.04, giờ nhân vật đứng trên sàn.

Converter cũng đã ghi chú rõ ở chỗ ghi root translation: **đừng** ground ở đó, và trỏ sang groundClamp.

### Nghi phạm phụ, CHƯA đụng

`<ContactShadows>` (drei) render vào một **ô vuông cứng** `scale: 5`, `far: 3`, cố định tại gốc và
**không đi theo nhân vật**. Nhân vật ra khỏi ô đó → bóng tiếp xúc bị cắt thẳng băng; cao quá `far: 3`
→ mất bóng. Đây khớp mô tả hơn cả shadow map. Chưa sửa vì cần Owner xác nhận đang nhìn thấy ở model /
pose / góc camera nào.

### Bug của chính tôi, đã sửa trong lúc làm

Bản đầu throttle fit 100ms → có frame subject trôi ra ngoài frustum (overshoot 0.06u). Bỏ throttle
(đọc ~50 bone/frame là microseconds). Overshoot đo được sau đó (tới 1.6u) hoá ra là **artifact của
phép đo**: probe lấy mẫu ngay khi đổi clip, lúc nhân vật vừa nhảy pose mà fitter chưa chạy. Thêm 600ms
settle → **0/22 mọi tổ hợp**. Giới hạn 1-frame-lag đã ghi vào đầu file `shadowFit.ts`.

Regression sau cùng: flash **0/1148 frame**, FSM **11/11**, `tsc` + `build` xanh.

## Còn lại

- **§9 facial↔body sync** — chưa làm (đúng scope). Chỗ cắm đã sẵn: `facialOf(state)`, `stateChanged`
  emitter, thứ tự `useFrame` body → facial → `vrm.update`.
- **P0 ALB Kimodo** — chờ Owner/N (cần AWS creds, ~$24/ngày). K không tự chạy.
- **P2 Kimodo runtime delivery** — `playMotionFile` đã sẵn, backend chưa stream motion URL.
- Chưa commit gì — chờ lệnh Owner.

[[29-07-2026]] · [[animation-fsm-refactor]] · [[status]]
