---
date: 2026-06-12 (cont.)
phase: m9-closeout
branch: feature/langgraph-rewrite
participants: [K (Architect — spec FIX-M9-CLOSEOUT.md), N (Developer — implement)]
mode: implementation (Cụm A: A0→A6 per spec)
---

# 12/06/2026 (cont.) — §M.9 Closeout + Security Patch (Cụm A)

## Context

K viết spec `FIX-M9-CLOSEOUT.md` (sau `PREDEPLOY-AUDIT.md` Phần 2-A). 6 tasks
để closeout §M.9 + vá security path traversal. N implement toàn bộ A0-A6.

Package đã được restructure: `agenticRAG/agentic_rag_gemini/langgraph_agents/`
→ `agenticRAG/langgraph_agents/`. Sửa 1 import cũ (`core.circuit_breaker` →
`langgraph_agents.core.circuit_breaker`).

---

## A0 — Path traversal `persona_id` (SECURITY)

### Defense in depth 2 tầng

1. `api/schemas.py::ChatRequest.persona_id`: thêm `Field(pattern=r"^[A-Za-z0-9_-]{1,64}$")`
   → Pydantic chặn từ cửa, trả 422 nếu persona_id chứa `/`, `\`, `..`
2. `nodes/_persona_loader.py::_load_persona`:
   - Validate `persona_id` bằng `re.fullmatch()` trước khi dùng làm path
   - `Path.resolve()` + `.relative_to(personas_dir.resolve())` containment check
   - Fail → fallback persona, KHÔNG đọc file ngoài

### Tests

`test_a0_persona_security.py` — 4 tests:
- `../../../README` → fallback
- `a/b`, `a\b`, `..%2F..%2Fx` → fallback  
- `eca_default`, `eca_clinical`, `eca_friendly` → load đúng
- Cache không polluted bởi invalid key

---

## A5 — Chuyển task registry

`_pending_summarizer_tasks` từ `api/main.py` → `nodes/summarizer.py` (module-level).
Xóa lazy-import ngược từ summarizer vào api/main.py.

---

## A6 — Test nits

- `test_resume_last_session_schema_hides_user_id`: bỏ `or True` no-op, assert `since_days in fields`
- `test_memory_search_tenant_isolation`: bọc test trong `try/finally` → cleanup kể cả khi assert fail

---

## A3 — user_memory write path (D14 MVP)

### Endpoints mới

| Method | Path | Hành vi |
|---|---|---|
| `POST` | `/users/{user_id}/memory` | Body `{fact_text, category?}`. Validate 1-500 chars. Ensure user row → INSERT → return `{id, fact_text, ...}` |
| `GET` | `/users/{user_id}/memory` | List facts `valid=true`, newest first, limit 50 |
| `DELETE` | `/users/{user_id}/memory/{fact_id}` | Hard delete, verify fact thuộc user → 404 nếu không |

### Schemas mới

`api/schemas.py`: `UserMemoryCreate`, `UserMemoryItem`, `UserMemoryListResponse`

---

## A1 — Clarify động M.2b (tool ambiguity metadata)

### resume_last_session
- Fetch TOP 2 sessions (không phải 1)
- Nếu ≥2 sessions VÀ top-2 `updated_at` gap < 24h → `ambiguous: true` + `candidates` (max 3)
- Mỗi candidate có `session_id`, `preview` (first user msg 80 chars), `updated_at`

### memory_search
- Sau khi có results, check top-2: khác `session_id` VÀ `similarity[0] - similarity[1] < 0.05` → `ambiguous: true` + `candidates` (summary_text 80 chars + session_id)

### Synthesizer
- `_CLARIFY_TASK`: thêm `{tool_results}` parameter
- Node: pass `tool_results` vào clarify mode (trước đây clarify không nhận tool results)

---

## A2 — GDPR wiring M.8

### Endpoints mới

| Method | Path | Hành vi |
|---|---|---|
| `DELETE` | `/sessions/{session_id}/messages/{message_id}?user_id=` | Verify ownership (message → session → user khớp) → `gdpr.delete_message` → fire background `re_summarize_chunk` cho từng chunk dirty |
| `DELETE` | `/users/{user_id}` | `gdpr.delete_user` (FK cascade) → xóa Redis `stm:{sid}` của mọi session user |

Cả 2 dùng pattern `asyncio.create_task` + `_pending_summarizer_tasks` (giống summarizer M.5).

---

## Files changed

| File | Change |
|---|---|
| `api/schemas.py` | A0: persona_id pattern + A3: UserMemory schemas |
| `nodes/_persona_loader.py` | A0: path validation defense-in-depth |
| `nodes/summarizer.py` | A5: `_pending_summarizer_tasks` module-level |
| `api/main.py` | A3: 3 user_memory endpoints + A2: 2 GDPR endpoints |
| `tools/pgvector_tool.py` | A1: ambiguity metadata in resume_last_session + memory_search |
| `nodes/synthesizer.py` | A1: tool_results in clarify mode |
| `tests/test_a0_persona_security.py` | **NEW** — 4 path traversal tests |
| `tests/test_pr1_memory_fix.py` | A6: fix no-op assert + try/finally cleanup |
| `tests/test_phase6_circuit_breaker.py` | Fix import path (package restructure) |

## Test results

```
208 passed in 211.54s
  - 179 unit tests + 4 A0 tests + existing integration tests
  - All existing behavior preserved
```

## Còn defer

- A4 (verify E2E thủ công) — cần services chạy thật (SearXNG :6666, LLM, 10k+ conversation)

---

## K REVIEW (12/06 tối) — ❌ REQUEST CHANGES (2 điểm, 1 blocking)

Đã verify: A0 ✅ (validate→containment→read, đúng thứ tự; 4 test pass) · A1 ✅ (cả
memory_search lẫn resume_last_session đúng tiêu chí 24h/0.05, candidates đúng shape;
synthesizer clarify nhận tool_results) · A3 ✅ (3 endpoints, ownership check DELETE đúng)
· A5 ✅ · A6 ✅ · 208/208 pass (K tự chạy lại xác nhận).

### R1 — BLOCKING: A2 re-summarize không bao giờ chạy

`api/main.py:317` gọi `re_summarize_chunk(chunk["id"], session_id)` — nhưng signature
thật trong `db/gdpr.py:185` là `re_summarize_chunk(session_id, chunk_id,
new_summary_text, new_embedding)` (4 args bắt buộc). Background task raise TypeError
→ bị nuốt im lặng (create_task không await) → **chunk dirty vĩnh viễn** → context
đoạn đó mất luôn khỏi memory + search.

Gốc rễ: `gdpr.re_summarize_chunk` chỉ là UPDATE — nó KỲ VỌNG caller đưa sẵn
summary_text + embedding mới. **Logic "tóm tắt lại từ messages còn lại" chưa ai viết.**
Fix yêu cầu: thêm `rebuild_dirty_chunk(session_id, chunk_id)` (đề xuất đặt trong
`nodes/summarizer.py` — tái dùng LLM prompt + embed_passage của M.5):
load messages trong `[covers_from_seq, covers_up_to_seq]` còn lại → LLM summarize
→ `aembed_passage` → gọi `gdpr.re_summarize_chunk(session_id, chunk_id, text, vec)`.
api/main.py fire task này thay vì gọi thẳng re_summarize_chunk.

### R2 — Thiếu acceptance tests A1/A2/A3 theo spec

Spec FIX-M9-CLOSEOUT yêu cầu ~13 test items cho A1/A2/A3; thực tế chỉ +4 test A0
(204→208). Tối thiểu phải có trước merge:
- A2: dirty-window test (xóa message giữa chunk → chunk dirty → memory node +
  memory_search loại nó) + re-summarize round-trip (sau fix R1) + empty-chunk +
  ownership 404 + delete_user cascade sạch
- A1: ambiguous true/false theo gap 24h; memory_search gap 0.05; `_derive_mode`
  → clarify khi ToolMessage có ambiguous
- A3: POST fact → memory node inject [USER FACTS]; ownership 404; validate 422

### R3 — nit (không chặn): test A0 #4 kết thúc bằng comment, không assert trạng thái
cache; persona cache phình không giới hạn với id hợp-pattern nhưng không tồn tại
(mỗi id lạ cache 1 fallback) — cân nhắc không cache fallback.

**Điều kiện merge**: R1 fix + R2 đủ test + suite xanh → K re-review.
