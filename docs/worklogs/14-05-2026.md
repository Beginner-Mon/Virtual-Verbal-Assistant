---
title: "Merge feature/double-rag vào develop"
date: 2026-05-14
tags: [merge, refactoring, docs, conflict-resolution]
---

# 14/05/2026 — Merge `feature/double-rag` vào `develop`

## Ngữ cảnh

Branch `feature/double-rag` chứa toàn bộ refactoring D1-D9 (8 module mới, 3 orchestrator đã patch, request ID propagation) và docs vault Obsidian (10 file .md). Công việc này chưa được merge vào develop từ trước.

## Thao tác

- **Git stash** toàn bộ thay đổi cục bộ trên develop (cleanup files cũ) trước khi merge
- **Merge** `feature/double-rag` → `develop` (commit `d745796`)
- **Xóa stash cũ** sau merge

## Conflict đã resolve

| File | Nguyên nhân | Cách giải quyết |
|---|---|---|
| `ECA_UI/index.html` | develop thêm `API_BASE_URL` prefix cho audioUrl; feature/double-rag thêm `exercises` + `showExerciseButtons` | Kết hợp cả hai: giữ `API_BASE_URL` prefix + thêm `exercises`/`showExerciseButtons` |
| `text-to-motion/DART/visualize_motion.py` | Hai INPUT_PATH khác nhau | Giữ phiên bản develop (mới hơn): `motion_15bd8e25-cd8.npz` |

## Kết quả

- **81 files thay đổi** (+7259/-2847 dòng) đã merge vào develop
- **`feature/double-rag`** được giữ lại làm backup
- **docs/ vault** (10 file .md + .obsidian config) đã có trên develop
- **8 module refactoring**: `agents/intents.py`, `agents/double_rag.py`, `agents/tools/base.py`, `agents/tools/registry.py`, `core/tracing.py`, `core/resource_guard.py`, `memory/vector_backend.py`, `core/circuit_breaker.py`
- **3 orchestrator đã patch**: `api_orchestrator.py`, `local_orchestrator.py`, `pipeline_orchestrator.py`
- **Request ID propagation** trong `api_server_pkg/app.py`
- **Unit tests**: `tests/unit/` với 5 file test mới

## Quy ước mới

Từ nay mọi thay đổi code đều được log vào `docs/worklogs/DD-MM-YYYY.md`. Mức độ chi tiết tùy theo tầm quan trọng và phạm vi ảnh hưởng của task.
