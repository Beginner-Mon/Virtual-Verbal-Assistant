---
date: 2026-08-28
tags: [worklog, kimodo, motion, dynamodb, queue, cloudfront, ci, bugfix]
author: K (via N)
branch: feature/kimodo-motion-queue
---

# Worklog 28/08/2026 — Hàng đợi motion Kimodo: đóng ba lỗi còn mở, mở CI cho hai thư mục test

## Bối cảnh

Nhánh `feature/kimodo-motion-queue` (28 commit, `45e867e..6c5569d`, 50 file,
+3344/−717) thay đường nối LangGraph ⇄ Kimodo từ **MCP over HTTP** sang **hàng đợi
DynamoDB**. Lý do và thiết kế nằm ở
`.claude/plans/hi-n-t-i-text-to-motion-kimodo-ang-joyful-wave.md`; phần cần nhớ:

- ALB tính tiền theo giờ kể cả khi Kimodo ở 0 → phải xoá/tạo thủ công mỗi lần.
  N muốn kiến trúc **tự** tối ưu chi phí, không phải quy trình nhớ-tay.
- Task ECS không có NAT, chỉ có gateway endpoint. Tài liệu AWS: interface endpoint
  tính tiền *"each hour ... irrespective of the state"*, gateway endpoint *"no
  hourly or data transfer costs — only supported by S3 and DynamoDB"*. **Đó là lý
  do hàng đợi là DynamoDB chứ không phải SQS.**
- GPU vốn tuần tự (đo được 16.36/24 GB VRAM trên A10G) → endpoint đồng bộ biến
  điều đó thành timeout; hàng đợi biến nó thành thứ nhìn thấy được.

Mười task đã xong ở phiên trước. Phiên này đóng ba việc còn mở mà quy trình review
đẩy lên cho người quyết, cộng một lỗ hổng CI phát hiện trong lúc làm.

## Đã làm

| # | Việc | Commit |
| --- | --- | --- |
| 1 | R28 — bỏ DART khỏi `pythonpath` dùng chung | `3646a55` |
| 2 | R27/R29 — `enqueue` ghi đè được row hết hạn | `789fc99` |
| 3 | CI chạy `tests/infra` + `tests/text-to-motion` | `6c5569d` |

### 1. R28 — DART che hai module đang sống

`ba136f9` bỏ `--override-ini` để cứu job LangGraph, và vô tình bỏ luôn thứ mà
override đó đang âm thầm làm: **thay cả danh sách `pythonpath`**, tức là giữ
`text-to-motion/DART` khỏi che `SpeechLLm`.

DART sở hữu `api_server.py` và `mcp_server.py` — hai tên mà SpeechLLm và Kimodo
cũng sở hữu — và nó đứng trước cả hai trong danh sách. Một tên top-level chỉ có
một ô trong `sys.modules`:

| `import ...` | Trước | Đúng ra phải là |
| --- | --- | --- |
| `api_server` | `text-to-motion/DART/api_server.py` | `SpeechLLm/api_server.py` |
| `mcp_server` | `text-to-motion/DART/mcp_server.py` | `text-to-motion/kimodo/mcp_server.py` |

Job SpeechLLm vì thế đi từ **44 passed → 21 passed / 17 failed / 13 errors**.

N chốt: DART là **backup cũ của Kimodo, out of scope** → bỏ khỏi `pythonpath`
thay vì viết code né nó. Suite DART tự mang root của mình trong conftest, vẫn thu
được 12 test khi chạy riêng. Hai suite **không bao giờ** chạy chung một tiến
trình pytest được — một tên, một ô.

**Đo lại:** `tests/SpeechLLm` → 44 passed / 7 failed, đúng baseline. Bảy lỗi còn
lại là `TTSRouter.__init__() got an unexpected keyword argument 'eleven_client'`,
có sẵn từ refactor TTS, không liên quan.

### 2. R27/R29 — row hết hạn khoá chết một prompt

`531812f` dạy `read_status()` tôn trọng `expires_at` rồi dừng ở đó. Hệ quả là một
row hết hạn **vừa không đọc được vừa không ghi đè được**:

```
read_status  → None (hết hạn)        → node bỏ qua nhánh cache_hit
enqueue      → row vẫn NẰM ĐÓ        → attribute_not_exists(job_id) fail → "exists"
node         → trả "queued" + job_id → nhưng row vẫn ghi status="done"
claim_next_job → chỉ query "queued"  → KHÔNG AI NHẶT
```

Prompt đó không dựng lại được cho tới khi TTL sweeper của DynamoDB ghé qua — AWS
chỉ cam kết *"within a few days"*. **Không có lỗi ở bất kỳ đâu.**

Mỉa mai: docstring của `recover_abandoned_jobs()` đã nêu đúng cái bẫy này cho
trạng thái `processing` — *"nếu row kẹt 'processing' mãi thì PutItem có điều kiện
sẽ luôn fail"* — mà không ai tổng quát hoá nó sang `done`.

**Luật giờ ghi thẳng trong docstring của `enqueue`:** *enqueue phải ghi đè được
đúng những row mà `read_status` từ chối trả về.*

```python
ConditionExpression=(
    "attribute_not_exists(job_id)"
    " OR attribute_not_exists(expires_at)"
    " OR expires_at < :now"
)
```

Hai trong ba trường hợp `read_status` trả `None`. Trường hợp thứ ba — row không
có `status` — **cố ý không phủ**: row duy nhất rơi vào đó là heartbeat, nó chưa
hết hạn, và mất nó thì mọi lời gọi `kimodo_node` trả `unavailable`. `job_id` thật
là 32 ký tự hex nên không bao giờ đụng `worker#heartbeat`; đối xứng không đáng
đánh đổi bằng rủi ro ghi đè đúng row đó.

**Hai hàm đọc cùng index có cùng điểm mù, sửa kèm:**

- `queue_depth()` đếm cả row hết hạn → biến `MAX_QUEUE_DEPTH` thành cái khoá: trả
  `busy` cho mọi người trong khi không có gì đang chờ.
- `claim_next_job()` nhặt row hết hạn — và row cũ **sắp trước** trên GSI
  (`created_at` nhỏ nhất), nên vòng lặp không kiểm tra sẽ chọn đúng nó mỗi lần.
  Tức là trả tiền GPU để dựng một yêu cầu 24 giờ tuổi.

  Lọc trong Python chứ không bằng `FilterExpression`: DynamoDB áp `Limit` **trước**
  filter, nên 5 row cũ sẽ trả về một trang rỗng và giấu mất job còn sống nằm sau.

**R29 — test xanh vì lý do sai.** `test_expired_done_row_re_renders_...` có ba
assertion và cả ba đều đúng vì lý do không liên quan: `!= "cache_hit"` đúng vì
node trả `queued`; `job_id` đúng vì nó tính từ HMAC bất kể có ghi được row hay
không; `Count == 2` đúng **chính vì lệnh ghi bị từ chối**. Thứ duy nhất nó không
bao giờ kiểm là row trong bảng thật sự nói gì. Giờ nó đọc row ra.

**Đo lại:** `test_motion_jobs.py` 24 passed; node/status/worker 18 passed; toàn bộ
572 passed, không lỗi mới.

### 3. CI mù với hai thư mục test

Không job nào chạm `tests/infra` hay `tests/text-to-motion`. Mọi guard viết cho
hàng đợi chỉ chạy trên máy dev — ai xoá gateway endpoint (interface endpoint tính
tiền theo giờ dù GPU tắt) hay xoá luật hết hạn đều sẽ thấy CI xanh.

Thêm matrix entry **"Motion Queue + Infra"**: không cần AWS credential (moto giả
DynamoDB + S3, CDK synth offline), không cần GPU (test worker chạy `run_once()`
với engine giả — cách duy nhất test được, vì **Kimodo không test local được**).
Loại trừ `tests/text-to-motion/DART` vì lý do ở mục 1.

`infra/requirements-dev.txt` giờ phải đứng một mình vì đó là thứ duy nhất job cài.
Kiểm bằng cách dựng **venv sạch** từ nó — và chính bài đó lòi ra thiếu `alembic`:
test migration 008 đang import nó nhờ venv dev tình cờ có sẵn.

**Đo:** venv sạch → 49 passed.

## Quyết định

| Quyết định | Vì sao |
| --- | --- |
| DART ra khỏi `pythonpath` thay vì đổi thứ tự | Đổi thứ tự chỉ lật xem ai vỡ. DART là backup cũ, out of scope (N chốt) |
| `enqueue` không phủ trường hợp "không có `status`" | Row duy nhất rơi vào đó là heartbeat. Đối xứng < an toàn |
| Ghi đè vẫn báo `"created"`, không thêm giá trị thứ ba | Câu hỏi của caller là "giờ đã có row queued cho tôi chưa" |
| Lọc expired trong Python ở `claim_next_job` | `Limit` áp trước `FilterExpression` → trang rỗng giấu job sống |
| Gộp infra + motion vào **một** matrix entry | Cùng bộ dep (moto/cdk), cùng không cần credential |

## Deploy + verify trên g5 (Task 12) — ĐÃ CHẠY

### Secret

| Thứ | Nơi | Ghi chú |
| --- | --- | --- |
| Khoá riêng RSA 2048 | SSM `/vva/motion/signing-key-pem` (SecureString) | round-trip fingerprint khớp `da5e2ac1…`; bản trên đĩa đã xoá |
| Khoá công khai | CloudFront PublicKey `KGUIRRSF1V55H` | trong KeyGroup `8332ab12…` |
| HMAC secret | SSM `/vva/motion/hash-secret` (SecureString) | 32 byte ngẫu nhiên |

Thứ tự deploy quan trọng và đã làm đúng: **`VvaAgentStack` với tag image CŨ trước**
(đặt env), rồi mới tới code mới. Code cũ không đọc các biến đó nên đặt sớm là vô
hại, và không có cửa sổ nào code mới chạy thiếu env.

### Năm lần deploy đỏ, năm nguyên nhân khác nhau

Không lần nào để lại hạ tầng nửa vời — rollback sạch cả năm.

| # | Đỏ ở đâu | Nguyên nhân | Sửa |
| --- | --- | --- | --- |
| 1 | `VvaAssetStack` | `-c motion_public_key_pem="$(cat …)"` → template nhận **26 ký tự**; giá trị nhiều dòng không sống qua argv trên Windows | `-c motion_public_key_file`, chuẩn hoá CRLF, guard hình dạng (`b2bfe6a`) |
| 2 | `VvaVpcStack` (chặn trước) | bỏ ALB/Service làm 5 export mất người dùng; CFN không cho xoá export đang được import | `export_value` (`13eaa81`) |
| 3 | `VvaKimodoEcsStack` | description của SG là **bất biến** → ép replace; tên cố định làm replace đụng chính nó | bỏ `security_group_name` (`0b21977`) |
| 4 | ASG launch | replace đã xoá `sg-0dfc71f3914a3bc7b`, mà launch template của ASG (**tạo tay ngoài CDK**) ghim theo id | launch template v4 + ghi chú (`3b2c53c`) |
| 5 | ASG launch | AWS hết capacity `g5.xlarge` ở `us-east-1a` — không phải lỗi của ai | ép thử lại; ASG đã trải sẵn 1a+1b |

Bốn lỗi đầu **cùng một hình dạng**: template đúng, nhưng không deploy được *từ chỗ
tài khoản đang đứng*. Test CDK chỉ synth và không bao giờ thấy trạng thái đã
deploy, nên không cái nào bị bắt. `cdk diff` đối chiếu tài khoản thật là bước
không thay thế được bằng unit test. Lỗi #4 tệ nhất vì nó **im lặng đúng kiểu dễ
nhầm**: CloudFormation báo sạch, ASG báo `Desired=1`, và lý do chỉ nằm trong
`describe-scaling-activities` — nhìn hệt như đang provision chậm.

### Cold start thật (đo được)

| Mốc | Giờ UTC | Δ |
| --- | --- | --- |
| Instance `InService` | ~13:14 | — |
| `run-task` | 13:20:26 | — |
| Task `RUNNING` (kéo image 3.26 GB) | 13:22:30 | 2:04 |
| Worker đập nhịp đầu tiên | 13:25:14 | 2:44 |

**Cold start ~5 phút từ run-task, không phải 38 giây.** Plan chỉ tính thời gian nạp
model. Thực tế entrypoint đồng bộ **17.8 GB** từ S3 mỗi lần bật:

- skeletons 241.8 MB @ 158.6 MB/s (1.5s)
- checkpoints 1131.2 MB @ 189.2 MB/s (6.0s)
- **text encoders 16 416.8 MB** ← phần lớn thời gian

Cache HuggingFace nằm ở `/mnt/instance-store/huggingface`, **ephemeral theo
instance**, nên không lần bật nào tái dùng được lần trước. Nếu muốn rút cold start
thì đây là chỗ duy nhất đáng động vào (EBS gp3 gắn kèm, hoặc bake vào AMI).

### Vòng đời một job

```
enqueue          → created
13:25:59 queued
13:26:03 processing     (+4s — poll 2s + claim)
13:26:06 done           (+3s render)
```

**7 giây từ enqueue tới done**, khớp ước tính 2.8–5.6s trong plan.

| Kiểm | Kết quả |
| --- | --- |
| `motions/<job_id>.bvh` | 63 592 byte (plan ước 64 KB) |
| `motions/<job_id>.npz` | 171 410 byte |
| `user_id` trên row | `gpu-verify` — ghi đúng |
| enqueue lại row `done` còn hạn | `exists` — cache hit, GPU không chạy lần hai |
| CloudFront **không** chữ ký | **403** |
| CloudFront **có** chữ ký | **200**, đúng 63 592 byte |
| CloudFront chữ ký **hết hạn** | **403** |

### Chi phí

Cửa sổ GPU ~16 phút (13:13 → 13:29), g5.xlarge on-demand ≈ **$0.27**. Teardown xác
minh ở bốn mức: ASG desired/instances, container instances, running tasks, và
`describe-instances` lọc `g5.xlarge` đang chạy.

### Chưa làm

`release` **chưa push** — Lambda `vva-agent` vẫn chạy image `a5028c8…` (code cũ,
chưa có `nodes/kimodo.py` phiên bản hàng đợi). Env và route `GET /motion/{job_id}`
đã sẵn sàng đón nó. Vòng đã chứng minh là worker → GPU → S3 → signed URL; mắt xích
agent tự enqueue chưa chạy trên production, nhưng có 31 unit test phủ.

## Còn lại trước khi deploy

**Điều kiện tiên quyết chưa làm** (thứ tự bắt buộc, `agent_stack.py:245-258` sẽ
fail synth nếu thiếu):

1. Sinh cặp khoá RSA cho CloudFront signed URL.
2. `ssm put-parameter /vva/motion/signing-key-pem` (SecureString, khoá riêng).
3. `ssm put-parameter /vva/motion/hash-secret` (SecureString).
4. `cdk deploy VvaAssetStack -c motion_public_key_pem="$(cat <pub>)"` — không có
   nó thì key group và behavior `motions/*` **bị bỏ qua hoàn toàn**, không báo lỗi.
5. `aws cloudfront list-public-keys` → lấy key pair id.
6. Push image worker (nhánh → `kimodo-release` → `deploy-production.yml`).
7. `cdk deploy VvaKimodoEcsStack VvaAgentStack VvaRestApiStack -c motion_key_pair_id=... -c agent_image_tag=<sha>`

**Task 12** — verify end-to-end trên g5, một cửa sổ duy nhất, teardown dù pass hay
fail. Chưa chạy.

**Task 11** (frontend) hoãn có chủ ý. Kèm theo: SSE chưa phát event `motion` (R26 —
`api/main.py:498-524` bắt `job_id` để lưu DB nhưng cố ý không phát, vì chưa có ai
tiêu thụ). Nghĩa là hôm nay motion dựng xong nằm trên S3 và **không ai báo cho
trình duyệt**.

**Ngoài phạm vi, đã ghi nhận:** `synthesizer.py:216` gom *mọi* `ToolMessage` vào
khối tool-evidence, không lọc theo `name`. JSON hàng đợi (`{"state": "queued",
...}`) vì thế bị dán vào prompt như thể là bằng chứng tra cứu, và
`_has_tool_results()` coi cả `{"state": "unavailable"}` là "có evidence" — đẩy một
lượt hỏi động tác không retrieval sang nhánh `synthesize` thay vì `chat`. Mâu thuẫn
với `synthesizer.py:11` (D26: *"synthesizer does NOT receive motion flag"*). N chốt
để ngoài đợt này.

## Liên quan

- [[hi-n-t-i-text-to-motion-kimodo-ang-joyful-wave]] — thiết kế + plan 12 task
- [[27-07-2026]] — phương án ALB bị bác
- [[langgraph-flow-persona]] — D3/D26, hard edge `needs_motion`
