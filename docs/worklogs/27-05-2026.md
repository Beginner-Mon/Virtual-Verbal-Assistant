---
date: 2026-05-27
phase: 6.10
branch: feature/langgraph-rewrite
---

# Phase 6.10 — Pre-Deploy Hardening

**Plan**: `PHASE-6.10-PREDEPLOY.md`
**Order**: 3 → 4 → 7 → 6 → 5 → 2 → 1 → 8

## Task 3 — Lock CORS Origins

- `api/main.py:107`: Replaced `allow_origins=["*"]` with env-based `_ALLOWED_ORIGINS` from `ALLOWED_ORIGINS` env var (default `http://localhost:3000,http://localhost:8080`)
- `.env`: Added `ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080`

## Task 4 — Log File Rotation

- `shared/logging.py:74-94`: `configure_root_logger` now adds `RotatingFileHandler` (10MB × 5 backups) when `LOG_FILE` env var is set
- `.env`: Changed `LOG_FILE=logs/agentic_rag.log` → `LOG_FILE=eca.log`

## Task 7 — Stop Generation (Disconnect Detection)

- `api/main.py:21`: Added `Request` to FastAPI imports
- `api/main.py:128-148`: `/chat` route now accepts `request: Request` and passes it to `_stream_chat`
- `api/main.py:211-229`: `_stream_chat` checks `await request.is_disconnected()` at each chunk — cancels graph when client disconnects

## Task 6 — SpeechLLm + SearXNG Health Checks

- `api/health.py:74-107`: Added `check_speechllm()` and `check_searxng()` — HTTP health checks against `VIENEU_URL/health` and `SEARXNG_URL/healthz`
- `api/health.py:113-114`: Wired both into `run_all_checks` parallel gather
- `.env`: Added `VIENEU_URL=http://localhost:5000` and `SEARXNG_URL=http://localhost:6666`

## Task 5 — TTS Audio Cleanup Cron

- Created `scripts/cleanup_tts_audio.py`: Deletes `.wav` files older than TTL (default 1h). Supports `--dry-run`, `--ttl`, `--dir`. Auto-detects VieNeu outputs dir.

## Task 2 — STM Token-based Sizing

- `nodes/memory.py:35-46`: Added `_STM_TOKEN_BUDGET = 1500` and `_select_stm_pairs()` helper — selects Q&A pairs from newest to oldest within token budget (rough estimate: 4 chars ≈ 1 token)
- `nodes/memory.py:67`: `_read_stm` now uses `_select_stm_pairs(all_pairs)` instead of `[-_STM_MAX:]`

## Task 1 — DB Normalization (Messages Table)

- `db/migrations/001_normalize_messages.sql`: Creates `messages` table (UUID PK, session_id FK, role CHECK, content, metadata JSONB, created_at) + index + `_migrated` flag column
- `db/migrations/migrate_messages.py`: One-time backfill script — copies `conversations.messages` JSONB → `messages` table rows
- `db/session_store.py`:
  - `write_session_turn`: INSERT into `messages` table (2 rows: user + assistant) instead of JSONB `||` append
  - `load_session_messages`: SELECT from `messages` with cursor-based pagination (`before` + `limit`)
  - `list_user_sessions`: LATERAL JOIN instead of `jsonb_array_elements`
  - Added `_coerce_metadata` helper
- `api/main.py:171`: `/sessions/{id}/resume` now accepts `limit` query param forwarded to `load_session_messages`

## Task 8 — YouTube Ingestion Pipeline

- `requirements-langgraph.txt`: Added `youtube-transcript-api>=0.6.0`
- Created `tools/youtube_ingest.py`: CLI script (`--url`, `--title`, `--channel`) → extract video ID → `YouTubeTranscriptApi.get_transcript` (vi/en fallback) → chunk (500 chars, 50 overlap) → embed → insert pgvector `source_type="youtube"`
- `tools/pgvector_tool.py:15`: Updated docstring to mention `source_type="youtube"`

## Tests

- `tests/langgraph_agents/test_phase6_health.py`: Added mock for `check_speechllm`/`check_searxng` in 3 tests (`test_health_detailed_all_ok`, `test_health_detailed_degraded_when_redis_down`, `test_run_all_checks_runs_in_parallel`)
- `tests/langgraph_agents/test_phase6_logging.py`: `test_configure_root_logger_sets_json_handler` now monkeypatches `LOG_FILE` env away
- 98 pass / 11 deselected (5 integration need PostgreSQL, 4 tts_result pre-existing Redis mock bug, 2 blocked by infra)

## Not Done (not blocking Phase 7)

- `DROP COLUMN messages` + `DROP COLUMN _migrated` from `conversations` — run after migration script verified + tests green with PostgreSQL running
- YouTube transcript retrieval integration into planner search_strategy (Phase 6.11+)

---

## Corrections (28/05 — K review)

### Correction 1 — Replace `metadata JSONB` with explicit columns

- `db/migrations/001_normalize_messages.sql`: Replaced `metadata JSONB` with `intent TEXT`, `tokens INT`, `grader_result TEXT` — 3 fixed columns instead of JSONB
- `db/migrations/migrate_messages.py`: Backfill now inserts NULL for intent/tokens/grader_result (old JSONB data doesn't have these fields)
- `db/session_store.py`:
  - `write_session_turn`: Added `grader_result="pass"` param, column-based INSERT (no JSONB)
  - `load_session_messages`: SELECT intent/tokens/grader_result instead of metadata column
  - Removed `_coerce_metadata` helper
- `api/main.py`: Passes `grader_result` from `final_state` to `write_session_turn`

### Correction 2 — `POST /sessions/{id}/resume` → `GET /sessions/{id}`

- `api/main.py`: Changed route to `GET`, removed `populate_stm_from_messages`, `stm_populated=False`
- `ECA_UI/test-ui/app.js`: Changed fetch from `method: 'POST'` to simple GET

### Correction 3 — STM lazy populate at `/chat`

- `api/main.py`: Added lazy STM populate at start of `/chat` — if STM empty, loads last 6 messages from PostgreSQL and populates Redis. Wrapped in try/except for graceful degradation when PG is down.
- `tests/langgraph_agents/test_phase5_sse.py`: Fixed `mock_redis.get` from `MagicMock` to `AsyncMock(return_value=None)` to support eager await at `/chat` startup
