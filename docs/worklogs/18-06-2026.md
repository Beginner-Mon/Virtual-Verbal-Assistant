---
date: 2026-06-18
phase: auth-integration + frontend merge review
branch: feature/langgraph-rewrite
participants: [K (Architect — spec + review + RUNBOOK), subagent (Sonnet — implement)]
mode: subagent implementation (Owner: demo on new UI, skip auth OK, wire 3 pieces now)
---

# 18/06/2026 — Frontend merge review + auth integration (frontend↔backend↔Cognito)

## Bối cảnh
- `feature/frontend` merged (710e5bd): React 18 + Vite + Cognito auth gate + VRM avatar.
  K review: **UI scaffold only** — ChatPanel mock, zero backend calls, JWT chưa gửi backend →
  IDOR chưa đóng. Old vanilla UI dời sang `ECA_UI/test-ui/sse-test/`.
- Owner 18/06: demo bằng UI mới (bỏ qua auth được), nối 3 mảnh NGAY. K spec `FIX-AUTH-INTEGRATION.md`.

## Quyết kiến trúc (K): auth ENFORCED-WHEN-PRESENT, không bắt buộc
Cờ `REQUIRE_AUTH` (default false): token hợp lệ → `user_id=sub` (bỏ client value);
không/invalid token + false → fallback client user_id (demo chạy); + true → 401 (IDOR đóng).
→ Máy móc ship ngay, bật cờ là đóng IDOR, không chặn demo.

## Thực hiện (subagent, K giám sát)
**Backend**: `api/auth.py` (resolve_user_id: PyJWKClient JWKS + jwt.decode RS256 +
audience/issuer/token_use=id; startup guard khi REQUIRE_AUTH=true thiếu config). Wired vào
/chat + /sessions (list/get/delete) + /users/*memory + delete message/user. CORS thêm :5173.
PyJWT[crypto] vào requirements. +8 test (test_auth.py) gồm IDOR-close path (REQUIRE_AUTH=true
+ no token → 401).
**Frontend**: `src/lib/api.ts` (streamChat SSE thật port từ test-ui/sse-test/api.js, authHeader
idToken, currentUserId sub/demo-UUID, sessions + user_memory CRUD). ChatPanel bỏ mock → streamChat
+ AbortController. Wire ChatPanel vào ChatPage (desktop 360px panel). `.env.example`.

## K review (verify, không tin báo cáo)
- Đọc `auth.py`: logic đúng + an toàn — verify đủ (RS256/aud/iss/token_use), fail-closed khi
  REQUIRE_AUTH=true, fallback đúng khi demo. Không forge được nếu thiếu private key pool.
- Đọc main.py wiring (`uid = await resolve_user_id(request, req.user_id)`) + api.ts (SSE/auth/endpoint khớp).
- **"3 failed" của subagent = MÔI TRƯỜNG, không phải regression**: Docker postgres+redis tắt
  (Docker restart ~1h trước; chỉ searxng có restart policy). Bật lại 2 container → tự chạy lại:
  **245 passed** (237 + 8 auth), 0 fail, 0 skip.
- Frontend `tsc -b` clean. `vite build` (production) fail vì thiếu `amplify_outputs.json`
  (pre-existing, cần `ampx sandbox`); `npm run dev` (demo) tolerate qua dynamic import + try/catch.
→ **APPROVE.**

## RUNBOOK cập nhật
§4 thêm khối Auth (REQUIRE_AUTH semantics) + 2 UI (React :5173 demo / SSE-test :3000);
§5 pytest 237→245 + note Docker-down; §6 mở UI :5173; port map + env toggles (REQUIRE_AUTH,
Cognito vars, ALLOWED_ORIGINS, VITE_API_BASE_URL).

## Trạng thái
TECH_DEBT 🔴 IDOR: fix có sẵn, gated — **phải set REQUIRE_AUTH=true + Cognito trước network deploy**.
Demo nội bộ: UI React :5173 chat thật qua backend :8080, không cần login. Chưa commit — chờ Owner.

## Còn defer
- Production `vite build` cần `amplify_outputs.json` (ampx sandbox) — chỉ ảnh hưởng build, không ảnh hưởng demo dev.
- Manual smoke `npm run dev` + browser: K chưa chạy (cần browser) — đề xuất Owner tự thử khi demo.
