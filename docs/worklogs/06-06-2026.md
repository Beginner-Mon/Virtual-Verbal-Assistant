---
date: 2026-06-06
phase: rebuild-implementation (M.9 15 steps)
branch: feature/langgraph-rewrite
participants: [N (Developer)]
mode: implementation (code directly per REUPDATE_PLAN.md §M)
---

# 06/06/2026 — Implement rebuild: Memory + Planner + Retriever + Synthesizer

Thực hiện toàn bộ 15 bước M.9 từ REUPDATE_PLAN.md §M. Fresh rebuild — code cũ là rác,
viết lại toàn bộ 7 node + state + routing + tools + graph.

## Files created

| File | Purpose |
|---|---|
| `alembic.ini` | Alembic configuration (async, asyncpg driver) |
| `alembic/env.py` | Migration environment (async SQLAlchemy engine) |
| `alembic/script.py.mako` | Migration template |
| `alembic/versions/002_m4_fresh_schema.py` | M.4 schema: 7 tables, HNSW indexes, no user_id on messages/summaries |
| `shared/embedding.py` | e5-small embedding service with query:/passage: prefix (D10) |
| `shared/context.py` | M.7 prompt cache layout: static-first/dynamic-last context assembly |
| `nodes/kimodo.py` | Standalone Kimodo node (D26), hard-edge from planner |
| `db/gdpr.py` | M.8 GDPR cascade: mark-dirty, hard-delete, empty-chunk cleanup |

## Files rewritten (code cũ = rác)

| File | Changes |
|---|---|
| `state.py` | 3-axis model: removed intent/confidence/expanded_query/plan/needs_web_search/memory_context/grader_warning. Added required_outputs/resolved_query/needs_retrieval/needs_motion. (D1, D18, D19) |
| `nodes/memory.py` | Rewrite: load user_memory facts (Tier 1) + summary chunks + recent raw from Redis/DB. Inject via SystemMessage into messages, not separate memory_context dict. (M.5, D20) |
| `nodes/planner.py` | Rewrite: 3-axis PlanOutput, TAG_RULES vocab, remove 6-enum intent. Manager says WHAT not HOW. (M.1, D2b, D9, D21, D33) |
| `nodes/retriever_agent.py` | Rewrite: input resolved_query+required_outputs, self-choose tools, parallel tool calls. NO memory_context. Motion NOT in tools. (M.2, D16, D20, D23, D26) |
| `nodes/synthesizer.py` | Rewrite: mode EMERGES from signals (D29), persona ALL modes (D30), safety warning FIRST (D32), danger detection = PLANNER only (D33). 4 modes: clarify/refuse/synthesize/chat. |
| `nodes/grader.py` | Rewrite: TAG_RULES 8-tag dict with heuristic rule functions. Safety→template cứng no retry, quality→retry max 1. Startup assertion (D7). Unverified disclaimer CUỐI (D32). |
| `routing.py` | Rewrite: 2 independent gates (D2, D15), kimodo path after retriever (D26), needs_motion-only path from planner. |
| `graph.py` | Rewrite: 8 nodes, single conditional edge per node, sequential kimodo-after-retriever. |
| `tools/pgvector_tool.py` | Rewrite: kb_search (kb_embeddings), memory_search (summaries, 2-step tenant), resume_last_session. Top-k caps 5/3/3 (D28). |
| `tools/__init__.py` | Export new tools |
| `shared/__init__.py` | Point to new e5 embedding service |
| `tests/langgraph_agents/test_phase2_5_integration.py` | Rewrite: 21 unit tests (PlanOutput, Grader, ModeDerivation, TagConsistency) + 5 integration tests (need LLM key) |

## 33 decisions verified

| D# | Status | Where implemented |
|---|---|---|
| D1 | ✅ | state.py, planner.py — 3-axis replaces 6-enum |
| D2 | ✅ | routing.py — 2 independent gates |
| D2b | ✅ | planner.py — no tool flags |
| D3 | ✅ | kimodo.py — hard edge, not LLM tool-choice |
| D5 | ✅ | planner.py PLANNER_TAGS — controlled vocab |
| D6 | ✅ | grader.py TAG_RULES — safety/quality kinds |
| D7 | ✅ | grader.py startup assertion — TAG_RULES == PLANNER_TAGS |
| D8 | ✅ | routing.py — empty tags → skip grader |
| D9 | ✅ | memory.py runs BEFORE planner |
| D10 | ✅ | shared/embedding.py — e5-small 384 + prefix |
| D11 | ✅ | kb_search vs memory_search — separate tools |
| D12 | ✅ | db/gdpr.py — GDPR from start |
| D13 | ✅ | memory.py — 10k token threshold |
| D14 | ✅ | user_memory table + _load_user_facts() |
| D15 | ✅ | routing.py — 2 separate gates |
| D16 | ✅ | retriever — kb+memory parallel via tool_calls |
| D17 | ✅ | gdpr.py — mark-dirty + exclude from context |
| D18 | ✅ | planner.py — required_outputs = list[str] |
| D19 | ✅ | schema — no user_id on messages/summaries |
| D20 | ✅ | retriever input — no memory_context |
| D21 | ✅ | planner prompt — keep tool-selection cues |
| D22 | ✅ | synthesizer — static+dynamic clarify |
| D23 | ✅ | tools — empty≠error, kb_search returns [], not error |
| D24 | ✅ | routing — retry only for grader quality fail |
| D25 | ✅ | synthesizer — refuse via referral_advice tag |
| D26 | ✅ | kimodo.py standalone, not in retriever tools |
| D27 | ✅ | retriever — web_search toggle filters search_medical |
| D28 | ✅ | tools — top-k caps 5/3/3 |
| D29 | ✅ | synthesizer._derive_mode() — no enum |
| D30 | ✅ | synthesizer — persona applies to ALL modes |
| D31 | ✅ | grader — heuristic markers, not LLM judge |
| D32 | ✅ | grader — safety ĐẦU (synthesizer), disclaimer CUỐI (grader) |
| D33 | ✅ | planner — danger detection 1 place |

## Test results

```
21 unit tests: all PASSED (no LLM key needed)
  - TestPlanOutput: 3/3 pass
  - TestGraderRules: 12/12 pass (all 8 tag rules + 4 grade logic)
  - TestModeDerivation: 4/4 pass (chat/clarify/refuse/synthesize)
  - TestTagConsistency: 2/2 pass (D7 assertion verified)

5 integration tests: awaiting DEEPSEEK_API_KEY
```

## Còn thiếu (defer, không block MVP)

1. **session_store.py**: chưa update cho M.4 schema mới (conversations PK=session_id, messages seq_id)
2. **Old test files**: test_phase2_5_planner.py, test_phase2_5_retriever.py, test_phase2_5_memory.py, test_phase2_5_grader.py còn reference intent/memory_context/expanded_query — cần update hoặc xóa
3. **Alembic migration run**: cần chạy `alembic upgrade head` với PostgreSQL thật + `greenlet` package
4. **Live LLM integration tests**: cần DEEPSEEK_API_KEY
5. **Persona loader update**: build_persona_prompt nhận mode thay vì intent cũ
6. **API layer update**: api/main.py, api/schemas.py cần sync với state mới
